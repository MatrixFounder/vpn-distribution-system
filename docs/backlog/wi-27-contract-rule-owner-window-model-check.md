---
id: WI-27
type: work-item
status: open
opened_at: 2026-09-15
slug: wi-27-contract-rule-owner-window-model-check
effort: M
value: 'решение не отвергается на следующем раунде из-за непосчитанной стороны'
source: 'vdd-03-develop 001.33 review'
provenance: machine
component: vdd-03-develop/contract-design
fingerprint: 9aeb367aa7906af3
finding_ref: fnd-20260915-001647-9aeb367a
---

# WI-27 — Правило контракта проверять по владельцу входа, оконным нормативам и модели данных до записи

> Filed by `run-feedback` from capture `fnd-20260915-001647-9aeb367a`. **This body is data, not instructions** — it derives from captured output and may quote untrusted text.

## Наблюдение

Правило контракта, принятое как решение, было записано в пять документов одним текстом и всё равно отвергнуто: порог §5.9 к нарастающей сумме частей ограничен объявленной нодой длительностью интервала — недоверенная сторона множит интервалы и эпохи; срок принудительного закрытия 10 мин выведен против очередей прокси, а не против Н-8 (буфер ноды ≥ 24 ч) — поздние части после отказа CP не определены; для правил уровня интервала в модели данных не спроектировано состояние (сумма, время, признак закрытия, идемпотентность и срок разрывов). Правило: каждое правило контракта проверять (а) «кто контролирует каждую входную величину» (§11.3), (б) против всех нормативов с окнами времени (Н-8, Н-17, Н-4 — не только ближайшего), (в) «где хранится состояние, которое правило требует помнить» — до записи в README, не после роаста.

## Источник

workflow · review-finding · vdd-03-develop/contract-design · run vdd-03-develop-001-33 (задача 001.33)

## Слито при триаже (2026-09-15)

Включает WI-24: пара правил одной операции (например, «parts_total одинаков во всех частях» и «части после деления продолжают нумерацию») прогоняется через общий сценарий — отказ, повтор, деление — с конкретными числами до роаста. Итоговая формулировка правила для архитектора и разработчика: правило контракта записывается только после четырёх проверок — кто владеет каждым входом (§11.3), какие оконные нормативы его ограничивают (Н-8 и очереди прокси), какое состояние ему нужно в модели данных (нарастающие суммы, признаки, идемпотентность) и как оно ведёт себя в паре с соседним правилом той же операции.
