# Задача 001.80: Node Agent: применение конфигурации с перезапуском и горячее применение состава

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-01 Ввод ноды в эксплуатацию
- UC-05 Исчерпание лимита и отзыв доступа

Требования RTM: R-04.

<!-- contract:goal -->

## Цель задачи

Реализовать применение потока конфигурации (запись `/etc/xray/config.json`, финальное чтение
счётчиков, `systemctl restart xray`, новый `counter_epoch`, переустановка правил) и потока состава
(`AddUser`/`RemoveUser` без перезапуска; при снапшоте — удаление отсутствующих; тег `u{user_id}`).

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `node-agent/internal/sync/apply_test.go` — mock Xray: вызовы `AlterInbound`; перезапуск только при смене конфигурации

### Изменения в существующих файлах

#### Файл: `node-agent/internal/sync/apply_config.go`

- реализация

#### Файл: `node-agent/internal/sync/apply_users.go`

- реализация; вызов стратегии разрыва для состояний, снимающих доступ (001.60)

### Интеграция компонентов

Финальное чтение — 001.56; правила — 001.58; разрыв — 001.60.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Состав применяется горячо
   - Входные данные: дельта с двумя пользователями
   - Ожидаемый результат: `AddUser` дважды; перезапуска Xray нет (AC-32)
2. **TC-E2E-02:** Конфигурация с перезапуском
   - Входные данные: новая `config_version`
   - Ожидаемый результат: финальный отчёт отправлен; Xray перезапущен; правила переустановлены
3. **TC-E2E-03:** Снапшот удаляет лишних
   - Входные данные: снапшот без пользователя, который есть в Xray
   - Ожидаемый результат: `RemoveUser` вызван

### Модульные тесты

1. **TC-UNIT-01:** Тег пользователя
   - Проверяемая функция: `internal/sync::emailTag`
   - Входные данные: `user_id`
   - Ожидаемый результат: `u{user_id}`

### Регрессионные тесты

- Команда: `cd node-agent && go test ./internal/sync/... -run Apply`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] AC-32 выполнен
- [ ] Снапшот удаляет отсутствующих пользователей

## Примечания

`tdd-strict`.

Зависимости: 001.55, 001.56. Приоритет: Critical. Оценка: 4 ч. Этап: 9 — Node Agent (Go).
