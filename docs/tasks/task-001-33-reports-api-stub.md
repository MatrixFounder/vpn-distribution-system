# Задача 001.33: Node API отчётов, модули учёта и лимитов — сигнатуры и заглушки

Тип задачи: `[STUB CREATION]`.

## Связь со сценариями
- UC-04 Учёт трафика и списание с коэффициентом

Требования RTM: R-19, R-21, R-22, R-23, R-24, R-26, R-27, R-29, R-48.

<!-- contract:goal -->

## Цель задачи

Объявить `POST /agent/v1/reports` и `POST /agent/v1/quota/request` со схемами по
`docs/architectures/interfaces.md` §5.2, а также сигнатуры всех модулей учёта (`AccountingService`,
`multiplier`, `LimitsService`, `QuotaService`, сверки, агрегация, лимит адресов, признаки
перепродажи) с заглушками, чтобы логические задачи этапа 5 изменяли существующие файлы.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/app/agent_api/reports.py` — `POST /agent/v1/reports`: `{node_id?, counter_epoch, report_seq, parts_total, period_start, period_end, lines[{user_id, uplink_bytes, downlink_bytes}], online_ips[{user_id, ip, last_seen}], node_rx_bytes, node_tx_bytes}` → `{last_accepted_seq, duplicate}` (постановка писала `uplink`, `node_rx` без единиц и без `parts_total` — отклонения см. «Уточнения»); `POST /agent/v1/quota/request`
- `control-plane/app/body_shape.py` — пределы формы тела, выведенные из модели pydantic (скобки и запятые), и проверка по байтам до разбора JSON
- `control-plane/app/agent_api/body.py` — `BoundedBodyRoute`: класс маршрута всех роутеров раздела, проверка формы по байтам по модели тела каждой операции; сбой чтения тела — 400 единого формата
- `control-plane/app/accounting/service.py` — `class AccountingService`: `accept_report(node, report) -> Accept`; `close_hour(node_id, at)`; заглушки; схемы `ReportIn`/`ReportLine`/`OnlineIp`/`Accept`, пределы части и правила интервала (константы контракта)
- `control-plane/app/accounting/canonical.py` — канонические формы значений (`CanonicalUuid`, `Timestamp` по грамматике, `Address` без zone id) — общие для отчёта и гранта
- `control-plane/tests/e2e/test_reports.py` — приём отчёта на заглушке; фикстуры контракта (`contracts/agent_v1/report*.json`, `quota-request*.json`)
- `control-plane/tests/_reports.py` — строитель тел отчёта для трёх наборов тестов (`VALID_REPORT`, `report_with`, `widest_report`)
- `control-plane/tests/unit/accounting/test_report_schema.py`, `tests/unit/accounting/test_stubs.py`, `tests/unit/jobs/test_handlers.py`, `tests/unit/test_validation_answer.py` — модульные тесты (TC-UNIT-01…03 и потолок ответа 422); `tests/unit/test_proxy_contract.py` дополнен (TC-UNIT-04)
- `control-plane/app/accounting/.AGENTS.md` — карта пакета учёта (правило слоёв, граница транзакции приёма, бюджет части)
- `control-plane/app/domain/multiplier.py` — `async def resolve(conn, node_id, at) -> Resolved(multiplier_milli, billing_group_id, source)`; `def billable(raw: int, milli: int) -> int` — заглушки с фиксированными значениями
- `control-plane/app/accounting/limits.py` — `class LimitsService`: `check(user_id)` — заглушка
- `control-plane/app/accounting/quota.py` — `class QuotaService`: `grant(user_id, node_id) -> int`; `on_report(user_id, node_id, billable)` — заглушки
- `control-plane/app/accounting/reconcile.py` — `arithmetic(day)`, `cross_source(node_id, day, tolerance_pct)`, `continuity(node_id)` — заглушки
- `control-plane/app/accounting/aggregate.py` — `aggregate_day(day)` — заглушка
- `control-plane/app/accounting/device_limit.py` — `class DeviceLimitService`: `evaluate(user_id)` — заглушка
- `control-plane/app/accounting/signals.py` — `resale_signals(user_id, days=7) -> Signals` — заглушка
- `control-plane/app/jobs/handlers/limits.py` — обработчик `limits.check` — заглушка `noop` (реализовано как отказ без повторов `NonRetryableError` — отклонение от постановки, см. «Уточнения»)
- `control-plane/app/jobs/handlers/maintenance.py` — `partitions.ensure`, `partitions.drop_expired`, `retention.purge` — заглушки
- `control-plane/tests/unit/test_body_shape.py` — пределы формы каждой модели тела раздела литералами, отчёт — равенством с ручной формулой; `control-plane/tests/unit/accounting/__init__.py` — пакет модульных тестов учёта
- `contracts/agent_v1/report.json`, `report-continuation.json`, `report-node-mismatch.json`, `quota-request.json`, `quota-request-refused.json` — фикстуры контракта

### Изменённые файлы

- `deploy/nginx/nginx.conf` — `location = /agent/v1/reports` с пределом 1 МиБ, зоны и пределы одновременности и частоты (по сертификату, парковые на уровне server, по адресу у enrollment), буферы, равные пределам, 411 на тела без известной длины, `error_page` 411/429/5xx в едином формате, `proxy_request_buffering`/`proxy_buffering` явно, `proxy_max_temp_file_size`, пути временных файлов, таймауты апстрима, `worker_processes 1`, map Н-25 по `$uri`/`$request` и формат журнала для неразобранных запросов
- `deploy/compose/docker-compose.yml` — `mem_limit` = `memswap_limit`, `cpus`, `pids_limit`, tmpfs `/var/cache/nginx` (64 МиБ, 0711), `x-logging`
- `control-plane/app/errors.py` — потолок списка ошибок 422, усечение `loc`/`msg`, русский текст 400
- `control-plane/app/agent_api/state.py`, `enroll.py`, `heartbeat.py`, `commands.py`, `router.py`, `deps.py` — коды прокси в объявлениях операций (400/411/413/429/503; у GET без кодов про тело), `route_class=BoundedBodyRoute` на всех роутерах, фабрики служб учёта
- `control-plane/app/domain/composition.py` — комментарий у `STUB_QUOTA_GRANT_BYTES`; `control-plane/app/jobs/handlers/__init__.py` — регистрация обработчиков-заглушек
- `control-plane/tests/unit/test_proxy_contract.py`, `tests/contract/test_agent_v1.py`, `tests/e2e/test_agent_api.py` — стражи прокси и Compose, инвентарь фикстур, коды операций 001.28
- `contracts/agent_v1/README.md` — раздел «Отчёт о трафике и грант квоты», правила частей и интервалов, поведение агента по кодам, пределы прокси
- `docs/architectures/interfaces.md` (§5.1, §5.2, §5.4), `data-model.md` (§4.2.5 `traffic_reports`/`report_intervals`/`traffic_gaps`/`reconciliation_runs`, §4.3, §4.4, §4.5), `security.md` (§7.1–§7.3), `system-architecture.md` (C-09, правила границы 3–4), `deployment.md` (§10.4), `docs/ARCHITECTURE.md` (§8)
- `docs/tasks/task-001-{19,23,25,34,35,36,37,38,39,50,53,56,66,68,72,73,75,77}-*.md` — разделы «Найдено при 001.33, решить здесь»; `docs/PLAN.md` — строка статуса
- карты `control-plane/.AGENTS.md`, `control-plane/app/.AGENTS.md`, `contracts/.AGENTS.md`, `deploy/.AGENTS.md`
- закрытие: `control-plane/app/body_shape.py` — правила строк (доказанный алфавит или бюджет свободного текста) и предел серии цифр; `app/agent_api/enroll.py` — алфавит PEM у `csr_pem`; пять роутеров — `strict_content_type=True`; `app/accounting/service.py` — `REPORT_MAX_INTERVAL_AGE`; `deploy/nginx/nginx.conf` — 411 при отсутствии длины у любого метода, кроме GET и HEAD, Н-25 по пути до «?», `try_files … =404` и `error_page 404 = @not_found`, `client_header_timeout 5s`, `reset_timedout_connection on`; `docs/tasks/task-001-30-heartbeat-statuses-logic.md` — раздел «Найдено при 001.33»

### Интеграция компонентов

Схема отчёта — часть контракта `contracts/agent_v1/`.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Отчёт принят
   - Входные данные: валидный отчёт — первая часть и продолжение интервала
   - Ожидаемый результат: 200 `{last_accepted_seq: <report_seq принятой части>, duplicate: false}`
   - Примечание: заглушка без состояния — последний принятый равен только что принятой части

### Модульные тесты

Постановка объявляла их ненужными, но часть правил заглушки настоящая — их и проверяют:

1. **TC-UNIT-01:** Форма отчёта
   - Проверяемая модель: `accounting/service.py::ReportIn`
   - Ожидаемый результат: пределы размера литералами (ровно предел принят, на один больше —
     `too_long`), порядок границ и длина периода (не дольше часа; интервал через границу часа
     и дельта после перерыва приняты), `parts_total` с обеих сторон, канонические формы и
     строгие целые, уникальность пользователя в строках и
     пары «пользователь × адрес» (по разобранному адресу вместе с семейством, не по тексту),
     закрытость трёх уровней тела, границы ответа `Accept`; `user_id` запроса гранта — только
     каноническая форма; правило слоёв — домен не импортирует учёт (разбор импортов)
2. **TC-UNIT-02:** Сигнатуры и фиксированные значения
   - Проверяемые функции: все объявленные задачей модули учёта и `domain/multiplier.py`
   - Ожидаемый результат: сигнатуры совпадают с объявленными текстом (`inspect.signature`),
     заглушки не обращаются к пулу и подключению (часовые), правило «нода отчитывается только
     за себя» — 403 с `Retry-After`
3. **TC-UNIT-03:** Реестр обработчиков и расписание
   - Проверяемые данные: `jobs/handlers/__init__.py::HANDLERS`, `jobs/scheduler.py::SCHEDULE`
   - Ожидаемый результат: четыре новых типа зарегистрированы своими обработчиками, заглушки
     отказывают без повторов (`NonRetryableError`, тип в причине), не трогают подключение и
     не стоят в расписании; `ensure_partitions` остаётся
4. **TC-UNIT-04:** Пределы прокси и потолки Compose
   - Проверяемые файлы: `deploy/nginx/nginx.conf`, `deploy/compose/docker-compose.yml` и
     оверлеи из команды запуска (та же команда — в `deploy/README.md`)
   - Ожидаемый результат: предел уровня агентского `server` один и равен 64k с буфером, равным
     пределу; предел отчётов объявлен ровно в `location = /agent/v1/reports`, проксирующем на
     `$api`, с буфером, равным пределу, и выведен из пределов модели — самое большое допустимое
     тело (из самих пределов и наборов полей модели, в самых широких канонических формах)
     помещается, а предел не дальше 1 МиБ от него; тела без известной длины — 411 единого
     формата на обоих server Node API (map с двумя правилами — `Transfer-Encoding` при любом
     методе, любой метод, кроме GET и HEAD, без длины; `if`/`error_page` уровня server);
     зоны и пределы одновременности и частоты по отпечатку сертификата (на ноду и на парк;
     предел server повторён в `location` — не наследуется), по адресу источника и общий
     потолок у enrollment-server; каждый проксирующий `location` обоих server несёт зону по
     своему ключу и зону парка, зоны семейств — только на своём server; 429 в едином формате
     ошибки с `Retry-After`, таймаут тела — литералами и только на уровне server;
     `proxy_request_buffering on` явно и без переопределений (Н-4 по `urt`),
     `proxy_buffering on`, `proxy_max_temp_file_size` литералом, таймауты апстрима,
     `proxy_intercept_errors` не включён, `ssl_verify_depth 1`, `resolver_timeout`, уровни
     журнала пределов, `error_page` ни в одном `location`, `error_page 404 = @not_found` и
     `error_page 502 503 504 = @upstream_unavailable` на уровне обоих server с телами единого
     формата, `client_header_timeout 5s` и `reset_timedout_connection on` на уровне http;
     правила map 411 и Н-25 проверяются таблицами входов, а не текстом (эмулятор map: точные
     ключи без учёта регистра раньше регулярных); гейт бюджета Н-4 с бюджетом из строки Н-4
     постановки и окном разбора по обеим зонам; Н-25 — `access_log … if=$loggable` из map по
     `$uri` и по пути в `$request` до «?» без учёта регистра и формат без строки запроса для
     неразобранных 400;
     `log_format` несёт времена запроса и апстрима; у `api` и `nginx` в Compose — `mem_limit` =
     `memswap_limit`, `cpus` (равен `worker_processes` nginx), `pids_limit`, tmpfs
     `/var/cache/nginx` с размером из файла под потолком, проходимостью для рабочего процесса
     (биты прав) и обоими путями временных файлов под ним, `logging` с пределами у всех служб и
     ни в одном оверлее

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_reports.py tests/unit/accounting
  tests/unit/jobs/test_handlers.py tests/unit/test_proxy_contract.py tests/contract
  tests/e2e/test_agent_api.py tests/unit/test_validation_answer.py`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [x] Схемы зафиксированы в контракте `contracts/agent_v1/`
- [x] Сигнатуры всех модулей учёта объявлены; каждая отвечает без обращения к базе (фиксированным
  значением, кроме приёма отчёта: он отвечает номером принятой части без состояния — отклонение, см.
  «Уточнения»; и двух настоящих правил — сверки `node_id` и вентиля гранта)
- [x] Тест проходит на заглушках

## Примечания

Ограничения и допущения — `docs/idea.md` §9; архитектура — `docs/ARCHITECTURE.md`.


Найдено при 001.28 (заглушки), решить здесь:

- предел тела агентского `server` nginx снижен до 64 КБ по операциям 001.28 (тело читается до
  отказа по версии агента и identity, поэтому предел — защита от оплаты чужого мусора). Отчёты о
  трафике со строками по пользователям больше: поднимать предел надо **своим** `location =
  /agent/v1/reports`, а не на весь `server`. Страж `tests/unit/test_proxy_contract.py` требует,
  чтобы на уровне `server` остался ровно один предел, и покраснеет на попытке поднять его там.

Решено здесь: `location = /agent/v1/reports` в агентском `server` с собственным пределом тела
(`client_max_body_size 1m`) и буфером, равным пределу (`client_body_buffer_size 1m` — тело с
известной длиной лежит в памяти целиком, временного файла нет; тела без известной длины на
агентском и enrollment-server не принимаются — 411 `length_required` единого формата, так что
сырой поток с обрамлением без верхней границы до прокси не доходит; каталог временных файлов
прокси целиком смонтирован в Compose как tmpfs 64 МиБ под потолком памяти и нужен только
публичному server), `limit_conn` (один отчёт на ноду — части идут последовательно, два на
парк) и `limit_req` (2 в секунду на ноду, очередь 10); на уровне `server` — `limit_conn
agent_conn 8` на ноду, повторённый в `location` отчётов (location со своим `limit_conn` server
не наследует), `client_body_buffer_size 64k` (равен пределу — тела остальных операций тоже не
пишутся во временный файл), `client_body_timeout 10s` для всех операций, `ssl_verify_depth 1`,
частота остальных операций по сертификату (`agent_rate`: 5 в секунду, очередь 20) и на парк
(`agent_fleet_rate`: 15 в секунду, очередь 200); зоны — по `$ssl_client_fingerprint` и общие
с постоянным ключом `"agent"`; enrollment-server — по адресу источника (`enroll_addr_rate` 1 в
секунду с очередью 5, `enroll_conn_addr` 4) и общий потолок `enroll_fleet_rate` 2 в секунду;
429 и 411 — в едином формате ошибки, 429 с `Retry-After: 1`; страж `test_proxy_contract.py`
держит всё это литералами и положительным покрытием проксирующих `location`.

Уточнения при реализации (восемь раундов роаста; что менялось от раунда к раунду — в
`tests/tests-001/report-001-33.md`; решения по периоду, `parts_total` и способу измерения
бюджета приняты заказчиком перед пятым раундом, решения о семантике `parts_total` при делении,
о трактовке Н-4 по времени апстрима и о шестом раунде — перед шестым; перед седьмым заказчик
принял рекомендованные решения по четырём пунктам: порог §5.9 по стенным часам через
монотонные интервалы, сущность состояния интервала в модели данных и срок закрытия из Н-8,
запрет тел без известной длины на Node API, пределы enrollment по адресу источника и запрет
временных файлов ответов; перед восьмым заказчик принял все решения раунда 8 — байтовая
проверка формы на всех операциях, снятие запрета временных файлов ответов, удержанная часть
открывает свой интервал, якорь первого интервала, обработка смены эпохи, списание удержанного,
счётчики по направлениям, `interval_closed` в перечислении — и тем разрешил восьмой раунд):

- схемы тел и ответов обеих операций лежат в `app/accounting/` (`service.py`, `quota.py`,
  канонические формы — `canonical.py`, общий для обеих схем), а не рядом с маршрутами — как в
  001.28: домен возвращает их, обратный порядок сделал бы домен зависимым от слоя API;
- имена полей отчёта несут единицу: `uplink_bytes`, `downlink_bytes`, `node_rx_bytes`,
  `node_tx_bytes` (постановка задачи писала `uplink`, `downlink`, `node_rx`, `node_tx`). Та же
  величина в телеметрии уже названа `net_rx_bytes`; число без единицы вторая сторона обмена (Go)
  прочла бы по-своему. Соответствие колонкам §4.2.5 записано в README контракта
  (`uplink_bytes` → `raw_uplink_bytes`: сырой объём, списание считает Control Plane);
- `node_id` тела необязателен: ноду определяет identity запроса (§7.1), как у телеметрии 001.28.
  Предъявленный сверяется с нодой запроса — иначе 403 `node_mismatch` с `Retry-After: 3600`
  (ошибка конфигурации агента: буфер или идентификатор чужой ноды; повтор без оператора
  бессмысленен — пауза как у 426). Правило живёт в `AccountingService.accept_report`, а не в
  маршруте, и сверяет запрошенную ноду, а не константу (страж с нодой, отличной от
  фиксированной); в `details` — собственный идентификатор вызывающей ноды; `null` в теле
  равносилен отсутствию (Go шлёт `null` для незаполненного поля); ключ идемпотентности
  001.34 строит по ноде identity, никогда по полю тела (передано);
- `report_seq` — с единицы (`ge=1`): `last_accepted_seq: 0` в ответе означает «в этой эпохе
  ничего не принято». `last_accepted_seq` — номер последнего **принятого** отчёта: удержание
  до разбора (`held_anomaly`) его продвигает и отвечает 200 (отчёт сохранён, §5.9, нода
  освобождает буфер), отклонение по времени (`rejected_time`) — отказ 4xx без продвижения, и
  агент отклонённый отчёт не повторяет (разрыв номеров закрывает сверка непрерывности); части,
  полученные делением отказанной по размеру части (413/422), продолжают ту же нумерацию —
  номер переиспользуется, отказанная часть не принята и её ключ не записан. Оба исхода
  реализует 001.34, семантика закреплена здесь и в README, потому что по ней пишутся обе
  стороны обмена;
- период части — не дольше `REPORT_MAX_PERIOD` (час), **штатный интервал — 60 с, и это
  обязанность агента** (§5.9: от интервала выведен предел перерасхода Н-17, и он не меняется
  без пересмотра Н-17); длиннее — только дельта после перерыва снятия счётчиков (перезапуск
  агента при живом Xray): накопительные счётчики по часам не делятся, и отказ терял бы данные.
  Строка относится к часу UTC по `period_start` (§4.2.5) и получает коэффициент этого
  интервала (§4.9); интервал, пересёкший границу часа, целиком ложится в час начала — ошибка
  отнесения ограничена длиной интервала (штатно 60 с), а нода, снимающая счётчики на границе
  часа UTC, её не допускает вовсе (рекомендация README); перерыв дольше часа — новая эпоха
  счётчиков. Решение принято заказчиком после раундов 3–4: час без правил (раунд 3) давал
  ошибку отнесения до часа, пять минут (раунд 4) молча теряли дельту после перерыва и
  расходились с §5.9/Н-17. Раунд 6: обязанность 60 с Control Plane **наблюдает** —
  длительность каждого принятого интервала становится метрикой по ноде с алертом на
  систематическое превышение (передано 001.34/001.68), а «новая эпоха после перерыва дольше
  часа» **объявлена отклонением** от определения §5.9 (эпоха — при перезапуске Xray, базовая
  точка 0): эпоха здесь — базовая точка счётчиков агента, и смена эпохи без перезапуска Xray
  для сверки непрерывности — разрыв с оценкой объёма по полосе × длительность паузы (001.37),
  а не тишина;
- `parts_total` — объявленное число частей интервала (последняя — `report_seq` первой +
  `parts_total` − 1), строго целое, `1…REPORT_MAX_PARTS` (1 000; граница нужна, чтобы поле
  было ограничено с обеих сторон, как остальные): без него сервер не знал бы, завершён ли
  интервал (находка раунда 4, решение заказчика). Раунд 6 (решение заказчика — рекомендованный
  вариант): в пределах интервала число **может только расти** — деление отказанной по размеру
  части добавляет части, и правило «нумерация после деления» с ним больше не спорит (в
  раунде 5 «одно и то же во всех частях» делало интервал после 413/422 незавершаемым — CRITICAL
  линзы логики); действует последнее принятое, меньше прежнего — отказ по контракту (001.34);
  порог §5.9 (полоса × длительность интервала, известна с первой части) применяется к
  **нарастающей сумме принятых частей**, а не только к завершённому интервалу — нода,
  объявившая две части и приславшая одну, порога не обходит; новое число применяется до
  проверки диапазона, а `report_seq` + `parts_total` − 1 не выходит за `bigint` (правило
  модели). Раунд 7 (решения заказчика — рекомендованные варианты): порог держится **по стенным
  часам** — **интервалы ноды монотонны и не перекрываются** (и внутри эпохи, и через смену
  эпохи), `period_end` не позже часов сервера с допуском (001.34): сумма длительностей принятых
  интервалов не больше прошедшего времени, и нода не может ни размножить часовые интервалы, ни
  сбросить сумму сменой эпохи (CRITICAL линзы безопасности раунда 6: порог был ограничен
  длительностью, которую выбирает нода); слив буфера Н-8 после недоступности при этом
  законен. Интервал **закрыт** завершением, частью следующего интервала ноды (части идут
  подряд, буфер сливается по возрастанию `report_seq`) или сроком `REPORT_PARTS_DEADLINE` —
  **25 часов** (глубина буфера Н-8 плюс час предела интервала; в раунде 6 стояли 10 минут,
  выведенные из очередей прокси, а не из буфера ноды — нода, вернувшаяся после суточной
  недоступности Control Plane, получала бы отказ на законные части); часть к закрытому
  интервалу — отказ `interval_closed` без повтора, номер остаётся пропущенным, недостающие
  части — разрыв (001.34/001.37), двойного учёта нет; открытых интервалов у ноды — не больше
  `REPORT_MAX_OPEN_INTERVALS` (8; сверх — `held_anomaly` и алерт), смен эпохи — не больше
  `REPORT_MAX_EPOCHS_PER_DAY` (48) в сутки (смена эпохи для Control Plane неразличима с
  перезапуском Xray — исполнимое правило одно: разрыв между `period_end` прежней и
  `period_start` новой); состояние интервала — таблица `report_intervals` (data-model §4.2.5:
  `first_seq`, `parts_total`, `parts_accepted`, суммы принятых байтов, времена приёма, `closed_at`,
  `close_reason`; в раунде 8 суммы разделены по направлениям и добавлен признак `held`; нарастающие
  суммы — там, пересчёт по `traffic_lines` при 1 000 частей
  квадратичен), колонка `parts_total` и индекс `(node_id, period_end)` — там же, `traffic_gaps`
  получила уникальность `(node_id, gap_start, reason)`, партиции по дню и срок 90 суток; миграции —
  001.34/001.37;
- пределы одной **части** отчёта выведены из норматива Н-4 (Node API: p95 ≤ 200 мс) с учётом
  одновременности, по замеру стенда, а не рабочей машины. Раунды: первый — потолок «все 20 000
  пользователей в одном теле» (17,4 МиБ) стоил 0,86 с; второй — 5 000 / 25 000 (171 мс)
  укладывались только по одному; третий — 32 мс рабочей машины на стенде оказались 49 мс;
  четвёртый — p95 из 20 замеров на пустой VM (72 мс) с выбросом 168 и гейт, в котором пределы
  сокращались алгебраически. Пятый: 200 замеров через nginx под смешанной нагрузкой (поток
  heartbeat ~4/с и два потока отчётов двумя сертификатами) на теле из 6 000 элементов —
  медиана 45 мс, p95 88, максимум 171 (14,7 мкс на элемент): при 1 000 / 5 000 и двух слотах
  2 × 88 = 176 > 148 — пределы части снижены до `REPORT_MAX_LINES` 500 и
  `REPORT_MAX_ONLINE_IPS` 2 500; на этом теле (3 000 элементов) — медиана 26, **p95 50 мс**,
  максимум 113 (16,7 мкс на элемент — доля накладных на запрос падает с размером, гейт берёт
  большую). Шестой (решение заказчика — рекомендованный вариант): **Н-4 считается по времени
  апстрима `urt`** — прокси буферизует тело целиком до передачи, заливка при полосе ноды (0,07
  с на 50 Мбит/с) в норматив не входит и записана в README как обязанность таймаута агента
  (сквозное время законной части превышает 200 мс — это не таймаут); накладные прокси 2 мс
  (p95 `rt − urt` на петле VM) из бюджета убраны как внешние по отношению к `urt`, а в бюджет
  вошла **самая дорогая из остальных операций перед отчётом** — путь отказа 64-КиБ тела
  heartbeat из лишних ключей: **13 мс** по p95 `urt` (200 замеров; enrollment — 8 мс, 100
  замеров). Проверка модели «два слота = 2 × p95»: сто пар частей потолка, стартующих в один
  момент, — p95 второй из пары **69 мс** ≤ 100; один поток того же тела — p95 46 мс (константа
  16,7 мкс оставлена по большему замеру раунда 5); heartbeat под парами — p95 5 мс, максимум
  126. `urt` включает установку соединения с апстримом и ответ — стоимость завышена в
  безопасную сторону, и в 001.73 это названо (перемер — сквозного `urt`, не «разбора»). Раунд 7:
  стоимость остальной операции перемерена на самом дорогом теле — 64 КиБ из **минимально
  коротких** лишних ключей (≈8 000 ошибок pydantic; раунд 6 мерил ключи подлиннее): heartbeat
  **24 мс**, enrollment **17 мс** по p95 `urt`; в бюджет части входит не одна операция, а все,
  что очередь парка выдаёт за окно разбора двух частей плюс одна начатая до него —
  `ceil(0,1 с × 8 в секунду) + 1 = 2`; трактовка Н-4 по `urt` закреплена явным
  `proxy_request_buffering on` со стражем (в раунде 6 держалась на умолчании nginx). Гейт
  (`tests/unit/test_proxy_contract.py::test_the_fleet_ceiling_keeps_the_parse_budget_of_h4`)
  считает от **константы стоимости элемента** 16,7 мкс (согласована с замером на измеренном
  теле отдельным ассертом; измерена при отношении строк к адресам 1:5 и при 3 000 элементах —
  гейт требует равенства пределов модели измеренному телу, иной состав означает перемер,
  001.39), а не от пределов, которые он держит (частное от них сокращалось бы — находка
  раунда 4): 3 000 × 16,7 мкс × 2 слота + 3 × 4 + 2 × 2 = 116 мс ≤ 200 − 50 (закрытие — 4 мс на операцию, результат команды с
  бюджетом свободного текста; раунд 8 — 110 при 2 мс; в раунде 7
  стояло 2 × 24 = 148 при 8 r/s; бюджет 200 читается из
  строки Н-4 `docs/idea.md`; резерв на транзакцию приёма 001.34/001.77 — оценка без замера,
  помечена так, снижается только по замеру 001.73 — ассерт нижней границы, пол закреплён);
  константы операций равны записанным замерам (ассерты равенства); вторая половина гейта —
  доля цикла, разрешённая остальным операциям (четверть — решение, записанное в §5.2, закреплено
  ассертом): частота парка (`agent_fleet_rate`) × 2 мс плюс enrollment (`enroll_fleet_rate`) × 2 мс
  ≤
  четверти цикла (15 × 4 + 2 × 2 = 64 ≤ 250 мс в секунду; штатно парк §5.7 даёт ~5 запросов в
  секунду); третья, «сумма всех классов на предельных потолках прокси ≤ цикла» (раунд 8:
  16,2 части/с × 50 мс + 34 = 846 ≤ 1 000), снята при закрытии — она считала знаменатель по
  самой широкой части и была зелёной только на ней (компактная законная часть давала 1 050),
  а при потолке одновременности 2 цикл на насыщении занят отчётами целиком по замыслу;
  задержку держит оконная модель. Раунд 8 (решение
  заказчика): в раунде 7 запаса у гейта не было (148 из 150 мс), а линзы показали, что окно не
  считало enrollment, «худшее» тело не было худшим (трёхсимвольные ключи), а сумма классов
  превышала цикл; лечение — **байтовая проверка формы на всех операциях агента**
  (`app/body_shape.py`: пределы скобок и запятых выведены из модели каждой операции
  рекурсивно, `agent_api/body.py`: один класс маршрута на все роутеры раздела) — путь отказа
  64-КиБ тела из ≈8 500 минимально коротких ключей стоит **2 мс** по p95 `urt` (heartbeat
  ×200, enrollment ×60; в раунде 7 без проверки — 24 и 17), худшее тело отчёта в пределах формы
  (3 000 элементов по три лишних ключа, 18 000 ошибок pydantic) — **37 мс** ≤ 50 приёма (100
  замеров; линза оценивала 54); окно разбора считается по обеим зонам; частота парка возвращена
  к **15 r/s** (стенд: шесть сертификатов замкнутым циклом — 14,8 r/s, задержка p50 0,40 с);
  запас гейта — 40 мс (после закрытия — 34: самая дорогая остальная операция стоит 4 мс). Цена
  двух слотов —
  пропускная способность парка 2 / (заливка + разбор) — 16,2 части в секунду (слот занят с
  начала заливки: 0,07 с на 50 Мбит/с плюс 0,05 с разбора; один цикл разбирает не больше ~20 —
  в раунде 6 стояло «≈ 40», противоречившее обеим моделям) против 50 частей в минуту при
  §5.7 (20 000 пользователей на 50 нод — около 400 на ноду, одна часть; в раунде 4 §5.7 был
  прочитан как 50 × 20 частей — исправлено). §5.7 контракт переживает без изменений —
  **делением отчёта на части**: интервал задаётся тройкой `(counter_epoch, period_start,
  period_end)`, части идут с последовательными `report_seq`, одинаковыми границами и
  объявленным `parts_total` (правила выше); пользователь — в одной части (его строка не
  делится), адреса — в любых, порог §5.9 — к нарастающей сумме принятых частей (001.34),
  счётчики интерфейса `node_rx_bytes`/`node_tx_bytes` — дельты за интервал — несёт первая
  часть, продолжения шлют 0 (001.77 суммирует; продолжение с ненулевыми счётчиками и
  пользователь в двух частях — отказ по контракту, передано 001.34). Предел адресов — пять на
  строку в среднем как запас над типичной сводкой (1–3 адреса на пользователя), не вывод из
  Device limit: отчёт обязан нести **все** наблюдаемые адреса, иначе превышение лимита §4.13
  не обнаружить;
- **путь отказа не дороже пути приёма**: pydantic даёт по ошибке на каждое лишнее поле, FastAPI
  пересобирает список целиком, и только потом `errors.py` режет ответ до 50 — тело в 1 МиБ из
  131 000 лишних ключей стоило бы 131 000 ошибок (76 мс на рабочей машине) при потолке
  ответа, а 350 000 вложенных пустых массивов — 22 МиБ в `json.loads`. Поэтому роутер
  отчёта — `APIRouter(route_class=BoundedBodyRoute)` (образец «Custom Request and APIRoute
  class» FastAPI; у гранта — свой роутер без проверки: у него другая модель, а тело держит
  предел 64 КиБ): до разбора JSON тело сверяется по байтам с `BODY_MAX_OPENERS` (3 003) и
  `BODY_MAX_COMMAS` (9 007) — счётчиками самого большого допустимого тела, выведенными из
  констант модели (у любого допустимого тела открывающих скобок и запятых не больше: строки
  допустимых значений этих символов не содержат; тело с дублями ключей JSON модель приняла бы,
  но запятых у него больше — считается широким, контракт дублей не предусматривает); тело шире
  — 422 `too_wide` одной ошибкой за 0,3 мс `bytes.count`, и проверка стоит до разрешения
  зависимостей — широкое тело без identity получает 422, а не 401 (пределы формы публичны,
  записано в README и закреплено тестом). Худшее тело, которое проверка формы пропускает
  (500 пустых строк и 2 500 пустых адресов — 9 000 ошибок `missing`), — 422 за 11 мс на стенде,
  50 ошибок с остатком 8 950 (тест). Обрыв заливки — 400 единого формата с тем же текстом, что
  у неразобранного тела, на **всех** операциях с телом: FastAPI переводит `ClientDisconnect`
  в свой 400 (чтение тела у него под общим `except Exception`), маршрут отчёта читает тело
  раньше FastAPI и переводит сам; закреплено тестом на уровне ASGI по инвентарю OpenAPI (семь
  операций с телом, включая enrollment), посадка снимает перехват — 500. На стенде: 90 000
  лишних ключей под 1 МиБ — 422 за 7 мс сквозного времени (`urt` 3 мс), 450 000 вложенных
  скобок — 422 за 17 мс, 3 000 лишних ключей у гранта — 422 `extra_forbidden` с остатком.
  Негодный UTF-8 и целое длиннее 4 300 цифр FastAPI переводит в 400 — код объявлен у всех
  операций раздела с телом (`AGENT_ERRORS`, enrollment тоже; у GET состояния коды про тело не
  объявляются) с русским текстом единого формата (`errors.py`; строка FastAPI — из версии
  0.141.1 `requirements.lock`, обновление зависимости меняет контракт). Глубина вложенности
  предела рекурсии не достигает: разбор JSON Python 3.14 проходит 32 000 уровней (самое
  глубокое тело под 64 КиБ) без `RecursionError` — её ограничивают размер тела и проверка
  формы по байтам, а разобранный список получает 422 модели (проверено тестом, раунд 7);
- самое большое допустимое тело части — 457 369 Б (0,44 МиБ) в записи `json.dumps` с
  пробелами (0,42 МиБ компактно; в README — оба числа, экранирование ASCII в контракт не
  входит), число закреплено в страже, и стенд строит тело теми же правилами, байт в байт (в
  раундах 4–5 генератор стенда отставал на 69 Б); предел `location` — 1 МиБ, и страж требует
  `самое большое тело ≤ предел ≤ самое большое тело + 1 МиБ` (полоса выброшенного разбора:
  тела в ней приложение читает — шире самого большого по скобкам и запятым отвергает по
  байтам, остальное разбирает и отвергает по пределу модели). **Тела без известной длины на
  Node API не принимаются** (раунд 7, решение заказчика): chunked HTTP/1.1 и поток HTTP/2 без
  content-length нужны только враждебному клиенту (с ними у прокси сырой поток с обрамлением
  без верхней границы — расширения чанков «1;xxxx…», ведущие нули — временные файлы и
  усиление в тысячи раз; в раундах 5–6 это лечили буфером 7m и tmpfs 16 МиБ, но два
  сертификата ровным потоком расширений держали бы оба слота отчётов вечно — MAJOR линз
  безопасности и производительности раунда 6): map `$body_without_length` по методу, длине и
  `Transfer-Encoding`, `if … return 411` и `error_page 411 = @length_required` на уровне обоих
  server Node API — 411 `length_required` единого формата без `Retry-After`, `Content-Length`
  обязателен по контракту (README, §5.2, объявлен у всех операций с телом); буферы **равны
  пределам** (64 КиБ и 1 МиБ) — тело с известной длиной лежит в памяти целиком, временных
  файлов тел на Node API нет по конструкции; на стенде: chunked на отчёте и heartbeat и поток
  HTTP/2 без длины — 411 с единым телом, обычные запросы не затронуты. Временные файлы прокси
  целиком (`client_body_temp_path` и `proxy_temp_path`) лежат под `/var/cache/nginx`,
  смонтированным в Compose как **tmpfs 64 МиБ** внутри потолка памяти контейнера с правами 0711
  на точке монтирования (раунд 8: 0700 раунда 7 не давал рабочему процессу nginx пройти в
  собственные подкаталоги — публичный POST больше буфера получал 500 с `Permission denied`,
  стенд; страж проверяет биты прав по смыслу, а не литерал) — тела пишет туда только
  публичный server телами больше буфера 16k (пределы — 001.66); ответы апстрима (снапшот
  состава с credentials, страница подписки, панель) сверх буферов уходят на тот же tmpfs не
  больше `proxy_max_temp_file_size` 8 МиБ на ответ (64 МиБ = восемь ответов; в раунде 7 файлы
  ответов были запрещены вовсе — `proxy_max_temp_file_size 0` по MAJOR линзы безопасности
  раунда 6, — но так nginx держал апстрим на скорости клиента и втаскивал её в `urt`: снято
  решением заказчика в раунде 8, `proxy_buffering on` объявлен явно со стражем, исчерпание
  tmpfs — 500 тому запросу, которому не хватило места; каталог общий с телами публичного
  server, и анонимные заливки могут оставить без места ответ Node API — разделение 001.66/001.75,
  роаст раунда 8); страж сверяет
  оба пути с точкой монтирования, размер tmpfs из файла с потолком памяти и с
  `proxy_max_temp_file_size`. Потолки: `api` — `mem_limit` = `memswap_limit` 1g (замер контейнера на
  стенде: 108 МиБ
  в покое, 132 МиБ после смешанной нагрузки раундов 5–6, 104 после проб раунда 7, один процесс
  `--reload`; два worker'а прода — оценка ~264 МиБ базы; разбор части — ~3,7 МиБ, экстраполяция
  замера раунда 2), `nginx` — 256m (19…28 МиБ в покое по раундам, 38 после проб раунда 5 —
  худший замер, 9 после проб раунда 7; буферы равны пределам — 1 МиБ на отчёт, 64 КиБ на прочее
  соединение, tmpfs до 64 МиБ; в раунде 4 потолок стоял только у `api`, хотя тела держит
  nginx), `cpus` (nginx 1.0, api 2.0) и `pids_limit` (nginx 256, api 512; анонимные рукопожатия
  публичного server
  иначе морят api и PostgreSQL на том же хосте); swap — cgroup `memory.swap.max` = 0 у обоих
  (`docker inspect` показывает запрошенное, не действующее); журналы всех контейнеров
  ограничены (`json-file`, 10 МиБ × 3) — иначе анонимный клиент заполнял бы диск хоста строками
  журнала, а отказы пределов не пишутся в error.log уровня warn (`limit_req_log_level notice`
  — штатное троттлирование парка иначе вымывало бы журнал); все потолки разбираются стражем
  как YAML во всех файлах Compose, которые накладывает команда запуска (список — из её
  объявления в шапке базового файла, сверенного с `deploy/README.md`; любой другой `*.y*ml` в
  каталоге — отказ, `include:` и `extends:` запрещены, `deploy.resources` не применяется,
  оверлей не трогает пределы, `cpus`/`pids_limit`, `logging` и тома nginx); превышение —
  OOM-kill без автоматического перезапуска до 001.66 (передано 001.66 и 001.68).
  Тело строится из самих пределов модели и наборов полей (равенством: поле, добавленное в
  001.39, обязано пересчитать предел и счётчики формы). Чтобы у «самого большого тела» была
  верхняя граница по ширине, модель принимает только канонические формы (`canonical.py`,
  `BeforeValidator`): UUID — 36 символов с дефисами (не `urn:uuid:`, не hex-32 — pydantic
  принял бы; дефисы не на месте отвергает pydantic-core, и это закреплено) — и в отчёте, и в
  запросе гранта; метки времени — текст по грамматике `TIMESTAMP` (разделитель `T`, смещение
  `Z` или `±hh:mm`, до шести знаков дроби; не число и не строка из цифр — pydantic принял бы
  обе как UTC); `TIMESTAMP_MAX_CHARS` выведен из грамматики (19 + 7 + 6 = 32) и согласован с
  ней тестом — отдельной проверки длины нет (в раунде 5 она была недостижимым кодом); адреса
  — текст без zone id (`fe80::1%eth0` библиотека `ipaddress` принимает, `inet` PostgreSQL —
  нет; не число); целые поля запросов и ответов — `strict` (bool, float и строка — 422); пары
  адресов сравниваются вместе с семейством (`192.0.2.1` и `::192.0.2.1` — разные, как у
  `inet`);
- пределы одновременности — на прокси: тело читается до identity, и отозванную identity nginx
  до 001.25 (`ssl_crl`) не знает, поэтому держатель любого сертификата CA ограничен только
  здесь — один отчёт от ноды (части последовательно) и два от парка одновременно, не чаще
  двух в секунду от ноды с очередью десять без `nodelay` (слив буфера Н-8 замедляется, а не
  отклоняется), восемь соединений на ноду на весь агентский `server` (повторено в `location`
  отчётов — location со своим `limit_conn` не наследует server), остальные операции — не чаще
  пяти в секунду на сертификат с очередью двадцать и **пятнадцати в секунду на весь парк** с
  очередью двести (раунд 6: в раунде 5 стояло сто в секунду на незамеренных «5 мс», замер дал
  13 мс — 15 в секунду; раунд 7 снижал до восьми при 24 мс на путь отказа; раунд 8 вернул
  пятнадцать — путь отказа после проверки формы 2 мс, 15 × 2 + enrollment 2 × 2 = 34 мс в
  секунду ≤ 250, гейт; частота на уровне server — её наследуют и пути отказа 411/404; очередь
  сливается за 14 с — таймаут остальных операций у агента ≥ 30 с, long-poll состояния ≥ 60 с,
  README; потолка соединений на парк для них нет — long-poll 001.75 держит по соединению на
  ноду; два сертификата — одна нода в окне ротации Н-28, 2 × 5 = 10 из 15 — берут две трети
  частоты парка, три сертификата (3 × 5 = 15) — всю; остальные ждут очереди и получают 429:
  записано в security.md §7.3, лечение — отзыв 001.25 и пороги по identity 001.72; в раунде 7
  при 8 r/s хватало двух — развёртка после правок раунда 8 поймала возврат числа). Общие зоны парка
  — с постоянным ключом `"agent"`, а не
  `$server_name` (он `_` у всех трёх server, и та же зона на другом server делила бы ведро с
  парком). **Enrollment-server** — единственный анонимный вход — держит пределы **по адресу
  источника** (`$binary_remote_addr`: один запрос в секунду с очередью пять и четыре соединения
  — раунд 7, решение заказчика: в раунде 6 общее ведро `enroll_conn_total 8` позволяло восьми
  анонимным медленным POST остановить enrollment всему парку) и общий потолок два в секунду с
  очередью десять (зона `"enroll"`), буфер равен пределу 64 КиБ, те же 411 и 429 единого
  формата; в раунде 5 анонимный поток 64-КиБ тел в тот же цикл событий был самым дешёвым путём
  (MAJOR линзы безопасности); хеш токена и распределённый источник (ботнет с многих адресов) —
  001.72. `client_body_timeout 10s`
  стоит на уровне обоих server (все операции, не только отчёты); `ssl_verify_depth 1` — только
  листы, подписанные CA напрямую (при глубине 2 нода с листом без CA:FALSE множила бы
  отпечатки; профиль листа — 001.25). Сверх пределов — 429 в едином формате ошибки с
  `Retry-After: 1` (`error_page 429 = @rate_limited` на обоих server: §5.2 требует `Retry-After`
  у отказов, и агент разбирает один формат) — на любой операции раздела; 429 объявлен у всех
  восьми операций, коды про тело (400, 411) — у семи с телом и ни у одной без него (GET
  состояния их не производит — `AGENT_ERRORS_WITHOUT_BODY`), 413 и 503 — у всех восьми (413
  достижим по `Content-Length` и у GET; `state.AGENT_ERRORS`, enrollment — свои тексты). Проверено
  на стенде: потолок парка — пятью сертификатами (2 × 200 + 3 × 429),
  потоки HTTP/2 в одном соединении считаются отдельно (1 × 200 + 3 × 429 от одного
  сертификата), запрос в очереди `limit_req` слота `limit_conn` не держит (восемь одновременных
  соединений одним сертификатом — 8 × 200 по очереди, а не 429; `limit_req` проверяется
  раньше); зона парка 15 r/s — шесть сертификатов замкнутым циклом heartbeat 20 с — 295 ответов 204,
  14,8 в секунду, задержка p50 0,40 с (раунд 7 на 8 r/s — ровно 8,0 и 0,74 с) — шестью
  клиентами очередь 200 не заполнить, предел парка проявляется задержкой, а 429 —
  только парку крупнее ~25 нод под залпом (001.73); enrollment — залп 30 с одного адреса:
  6 × 422 + 24 × 429, пять медленных заливок с одного адреса — одна 429 (`enroll_conn_addr
  4`); раунд 6 — залп 80 под общим ведром: 21 × 422 + 59 × 429. Повтор после 429 — с джиттером,
  начало интервалов — со случайной фазой (README, 001.53/001.56): иначе парк после
  недоступности штурмует два слота синхронно. Ограничение частоты по identity в приложении
  (001.72) этот слой не заменяет: оно стоит после разбора тела. Остаточный риск — медленная
  заливка держит слот отчётов до `client_body_timeout` между чтениями (предела на всю заливку
  у nginx нет): два сертификата с медленными заливками занимают оба слота парка — цена бюджета
  Н-4 против доступности учёта записана в 001.25, 001.72 и в таблице угроз security.md §7.3;
- ответ 422 ограничен `VALIDATION_ERRORS_MAX` 50 ошибками с остатком числом
  (`details.truncated`), строки `loc` усечены до `VALIDATION_LOC_MAX_CHARS` 64, сообщения — до
  `VALIDATION_MSG_MAX_CHARS` 200 (`app/errors.py`): имя лишнего поля — текст клиента, и
  пятьдесят ключей по десять тысяч символов (полмегабайта под пределом прокси) иначе
  вернулись бы эхом; `input` и `ctx` в ответ не копируются, как и прежде;
- что в заглушке настоящее: форма отчёта — границы колонок §4.2.5 (`0…INT8_MAX`, строго),
  канонические формы, `period_end > period_start` и период не дольше часа, `parts_total`,
  уникальность пользователя в `lines` и пары «пользователь × адрес» в `online_ips` в пределах
  ноды (первичные ключи `traffic_lines` и `user_online_ips` — отказ по контракту, а не ошибкой
  вставки в 001.77; сравнение по целым `uuid.int` и `(ip.version, int(ip))`), закрытость трёх
  уровней тела, проверка формы по байтам, окно признаков `resale_signals` 1…90 суток; вентиль
  §4.6 на обеих операциях через `Served` (`pending` — 403) и для гранта — по столбцу «Поток
  состава» (см. ниже), сверка `node_id`. Столбец «Учитывается трафик» матрицы влияния
  (`provisioning` — нет; `offline`, `disabled`, `suspended` — из буфера) заглушка не применяет:
  правильное поведение — принять отчёт, чтобы нода освободила буфер, и не тарифицировать, —
  это логика 001.34, а не отказ на входе;
- грант выдаётся только ноде, которой вентиль §4.6 открывает весь поток состава: грант —
  разрешение обслуживать пользователя автономно (§4.11), а ноде в `disabled`/`suspended` поток
  несёт только отзывы, в `provisioning` состава нет — 403 `grant_unavailable` с `Retry-After:
  300` (интервал опроса до перемены статуса: снятие `disabled`/`suspended` — действие
  администратора, `provisioning` → `active` — по применённой конфигурации и heartbeat) и
  статусом в `details`; правило живёт в `QuotaService.grant`, поэтому служба получает ноду
  целиком;
- отклонения от объявленных задачей сигнатур, каждое — по причине. Первый раунд ревью показал,
  что сигнатуры «по одному пользователю и без подключения» (`on_report(user_id, node_id,
  billable)`, `evaluate(user_id)`, `check(user_id)`) не могут лечь в транзакцию приёма отчёта:
  доставка отчётов at-least-once (§5.9), и подтверждение расхода гранта или запись блокировок
  на другом подключении при падении между коммитами потерялись бы навсегда — повтор отчёта
  ответил бы `duplicate`; а на потолке отчёта это ещё и тысячи обращений к базе внутри открытой
  транзакции. Поэтому: `QuotaService.on_report(conn, node_id, billable: Mapping[user_id,
  int])` — один вызов на отчёт на подключении приёма; `DeviceLimitService.evaluate(conn,
  node_id, user_ids) -> Blocked` (адреса сверх лимита по пользователю; окно читается вместе с
  только что записанными строками); `LimitsService.check(conn, user_ids)` — одна задача
  `limits.check` на отчёт с полезной нагрузкой `{"node_id", "user_ids"}` и ключом
  `limits:{node_id}:{counter_epoch}:{report_seq}` (внесён в interfaces.md §5.4; конструктор с
  пулом при вызывающем, имеющем только `conn`, — вопрос 001.35);
  `AccountingService.close_hour(conn, node_id, at)` — атомарно со сменой коэффициента в
  001.19; `QuotaService.grant(node, user_id, consumed_bytes) -> QuotaGrant` — нода целиком
  (вентиль в службе), грант целиком (`issued_seq` известен только службе), заявленный расход
  принят, чтобы маршрут не выбрасывал объявленное контрактом поле (назначение решает 001.36);
  функции модулей `reconcile.*`, `aggregate.aggregate_day`, `signals.resale_signals` принимают
  подключение первым аргументом, как `multiplier.resolve` и `stats.user_traffic`; обработчик
  `limits.check` объявлен постановкой заглушкой `noop`, реализован как отказ без повторов (см.
  ниже) — отклонение;
- службы-коллеги приёма (`QuotaService`, `DeviceLimitService`) приходят в `AccountingService`
  из графа зависимостей (`deps.get_accounting_service` ← `get_quota_service`,
  `get_device_limit_service`), а не создаются внутри: созданную внутри службу подмена в тесте
  не достигла бы (урок 001.28 для `CommandService` в `CompositionService`); без них конструктор
  создаёт свои над тем же пулом;
- `multiplier.resolve` отдаёт 1.0 из уровня `default` для фиксированной группы и вызывается
  один раз на отчёт (границы периода принадлежат отчёту, а не строке); `Resolved` держит
  диапазон 0…10000 и шаг 100 как инвариант; `billable` возвращает `raw` (коэффициент не
  применяется) — фиксированные значения по постановке задачи; `raw * milli // 1000` и таблицу
  случаев вводит 001.23 и снимает ассерт `billable(5, 3000) == 5` в
  `tests/unit/accounting/test_stubs.py`;
- сигнатуры всех объявленных функций закреплены текстом (`inspect.signature`) в
  `tests/unit/accounting/test_stubs.py`: логическая задача меняет их вместе с тестом, а не
  молча; заглушки не обращаются к пулу и подключению — часовые в тестах, как в контрактных
  тестах 001.28; по инвентарю роутера раздела (восемь операций) граф зависимостей каждой ведёт
  к `db_pool`, и каждая из трёх фабрик объявляет пул сама (страж: фабрики над `Depends`, урок
  WI-6, — иначе подмена пула в контрактных тестах не доходила бы до служб);
- обработчики очереди: `limits.check` (`jobs/handlers/limits.py`) и `partitions.ensure`,
  `partitions.drop_expired`, `retention.purge` (`jobs/handlers/maintenance.py`,
  `MAINTENANCE_TYPES`) зарегистрированы заглушками, которые **отказывают без повторов**
  (`NonRetryableError` → задача `failed` с типом в причине), а не завершают задачу успехом:
  зелёный «успех» отзыва по лимиту или удаления по срокам, которых не было, читался бы как
  выполненное обязательство (§16.2), а отказ виден в очереди и метриках; накопление `failed`
  в окне между 001.77 и 001.35 и срок хранения `jobs` — переданы 001.77 и 001.37. Обработчик
  `ensure_partitions` (001.14) остаётся и в реестре, и в расписании — он один создаёт и
  удаляет партиции до 001.37; заглушки в `SCHEDULE` не ставятся (страж
  `tests/unit/jobs/test_handlers.py`);
- `STUB_QUOTA_GRANT_BYTES` живёт в домене (`domain/composition.py`), служба грантов импортирует
  его оттуда: правило слоёв — учёт зависит от домена, домен от учёта нет (второй раунд
  перенёс константу в `accounting/quota.py` и тем создал ребро домен → учёт; третий вернул);
  страж правила разбирает дерево (`ast`: импорты в любой форме, относительные — от пакета
  файла, подпакеты, обращение по атрибуту после `import app`) и не считает только тело `if
  TYPE_CHECKING:` (ветка `else:` и `if not TYPE_CHECKING:` считаются — находка раунда 4) —
  так 001.36 аннотирует `QuotaService` в домене без `Any` (или через `Protocol`); в 001.36
  поток состава получит настоящий грант через службу из графа зависимостей;
- контракт: `contracts/agent_v1/` пополнен пятью фикстурами (`report.json` — первая из двух
  частей, `report-continuation.json` — продолжение того же интервала после деления с
  `parts_total`, выросшим с 2 до 3, другим пользователем и нулевыми счётчиками интерфейса —
  раунд 7, `report-node-mismatch.json`, `quota-request.json`, `quota-request-refused.json`;
  всего 21), набор `EXPECTED` контрактного теста и регулярное выражение таблицы README
  расширены (заглушка приёма отвечает номером принятой части, а не константой — иначе фикстура
  продолжения записала бы неверный ответ), README получил раздел «Отчёт о трафике и грант
  квоты» (ключ, нумерация и семантика `last_accepted_seq`, номер после отказа остаётся
  пропущенным — `max(last_accepted_seq, номер отказанной) + 1`, единицы и соответствие
  колонкам, канонические формы и строгие целые, уникальность с семействами адресов, штатный
  интервал и предел в час с отнесением к часу UTC начала, монотонные интервалы и порог по
  стенным часам, деление отчёта на части с `parts_total`, закрытие интервала и
  `interval_closed`, потолки открытых интервалов и смен эпохи, нумерация после деления,
  пределы прокси по сертификату на всех операциях и поведение агента на 400/403/411/413/422/429
  — 429 в едином формате с `Retry-After`, повтор с джиттером, 411 и обязательные
  `Content-Length` и `Content-Type`, таймауты агента 30 с / 60 с, экранирование и сжатие тела
  как решения отсутствия, грант и `issued_seq`) и правило `Retry-After` с двумя смыслами 403 —
  по нему агент на Go (001.53, 001.56) пишет свою сторону;
- источник истины: interfaces.md §5.1 — потолок списка ошибок 422, усечение `loc` и `msg`;
  §5.2 — коды 400, 403 (`node_not_approved`, `node_mismatch`, `grant_unavailable`), 409
  (`generation_mismatch`, `cursor_ahead` — из 001.28), 411 (тело без известной длины), 413 и
  429 прокси на всех операциях (коды про тело — у операций с телом), `Retry-After` у
  403/409/426/429 (а не у всех отказов — 400, 401, 411 и 422 его не несут), очередь прокси
  как задержка, оговорка о credentials только у строк, выдающих доступ (001.28), абзац о
  частях отчёта, монотонных интервалах, закрытии, периоде и канонических формах, политика
  четверти цикла для остальных операций; §5.4 — ключ `limits:{…}`; security.md — §7.1: ключ
  пределов enrollment — адрес источника, `ssl_verify_depth 1`; §7.2: тел без известной длины
  на Node API нет (411), буферы равны пределам, tmpfs на весь `/var/cache/nginx`, ответы апстрима
  сверх буферов — на tmpfs не больше 8 МиБ на ответ, swap не выдаётся ни nginx, ни api
  (cgroup), журналы ограничены, ответ 422 тело не повторяет; таблица угроз §7.3 — строки про
  части и порог, ложные отчёты (монотонные интервалы и порог по стенным часам), ложные адреса
  (вход лимита устройств — 001.38), отказ учёта парка двумя сертификатами (принятый
  компромисс; удержание слота честной медленной заливкой — остаток), зону парка как рычаг двух
  сертификатов, DoS enrollment по адресу (и NAT — остаток 001.72), дорогой путь отказа до
  identity (проверка формы каждой операции) и медленное чтение ответа (апстрим отпущен);
  system-architecture.md — правило границы 3 (`ssl_verify_depth 1`) и 4
  для C-09 (пределы на агентском server по сертификату и общие потолки парка зонами с
  постоянным ключом, 411 на тела без известной длины, пределы enrollment по адресу и общий
  потолок, политика четверти цикла, проверка формы каждой операции, буферизация тела и ответа
  явно, tmpfs временных файлов с файлами ответов до 8 МиБ, 411/429/503 в едином формате, 413 —
  страница nginx); data-model.md §4.2.5 — колонка `parts_total`,
  индекс `(node_id, period_end)`, таблица `report_intervals`, `traffic_gaps` с уникальностью,
  партициями и сроком (§4.5) (миграции — 001.34/001.37); deployment.md §10.4 — команда
  запуска с явными `-f`, потолки памяти/процессора/процессов, tmpfs, журналы, промышленный
  оверлей в той же команде;
- `agent_api/deps.py`: фабрики `get_quota_service`, `get_device_limit_service`,
  `get_accounting_service` над `Depends(db_pool)` и псевдонимы `Accounting`, `Quotas` — по
  образцу 001.28;
- свойство «каждая операция раздела требует identity и подтверждённую ноду» и свойство «у
  каждой операции объявлен 429 прокси, 413 и 503, у каждой операции с телом — 400 и 411, у
  операции без тела — ни тот ни другой» проверяются по инвентарю OpenAPI, а не по списку в тесте
  (`developer-guidelines` §6.3 п. 7); границы всех числовых полей отчёта, запроса гранта и
  ответов (`Accept`, `QuotaGrant`) — с обеих сторон по перечню полей с их собственными
  границами (`report_seq` и `parts_total` с единицы, `parts_total` до 1 000; в раунде 5
  `parts_total` в перечень не входил, в раунде 6 — `QuotaGrant`);
- стражи, которые оказались утверждениями об отсутствии, подстроке или тождеством (роасты
  раундов 3–4), заменены: потолки памяти читаются из всех файлов Compose как YAML для обоих
  контейнеров; правило слоёв — разбором импортов с разрешением относительных; закрытие Node
  API на публичном server — наличием блокирующих блоков `= /agent` и `^~ /agent/`; гейт
  бюджета — от константы стоимости элемента; разбор конфигурации nginx в тестах снимает
  комментарии вне кавычек, не считает скобки внутри строк и требует уникальных матчеров;
  `error_page` не допускается ни в одном `location`; харнес посадок засчитывает только имена
  тестов из `tests/` и отбрасывает строки журнала — красный от ошибки сбора считается
  неизмеренным. Раунд 6 закрыл оставшиеся дыры стражей: разбор nginx маскирует строки в
  кавычках при поиске блоков (фантомный `server` в значении заголовка, скобка в регулярном
  матчере), находит server по порту в любой записи адреса (`[::]:8443`) и запрещает `include`
  кроме таблицы MIME; список файлов Compose берётся из команды запуска, любой другой `*.y*ml`
  в каталоге — отказ, `include:`/`extends:` запрещены; страж слоёв видит обращение по
  атрибуту после `import app` и переименован честно (`…_never_reaches_accounting_at_runtime`:
  тело `if TYPE_CHECKING:` вне правила — 001.36); ветка обрыва заливки покрыта тестом на
  уровне ASGI; отдельной проверки длины метки нет — константа выведена из грамматики и
  согласована с ней тестом; в гейте бюджета каждый литерал держится ассертом (нижняя граница
  резерва, согласованность констант операций с замерами, модель пар против замера пар,
  посадки по обоим пределам части — L1 и L2 — и по частоте парка). Раунд 7 закрыл дыры
  стражей, названные линзами раунда 6: положительное покрытие — каждый проксирующий `location`
  обоих server Node API несёт зону по своему ключу и зону парка, зоны семейств — только на
  своём server (включая служебный 8081); `proxy_request_buffering on` явно и без
  переопределений; 411 — map с тремя правилами, `if` и `error_page` уровня server, тело единого
  формата; `proxy_max_temp_file_size 0`, `resolver_timeout`, уровни журнала пределов; бюджет
  Н-4 читается из строки постановки, доля цикла и пол резерва закреплены, число элементов
  части равно измеренному, модель окна разбора; `include` в любой записи и `listen *:порт`;
  команда запуска сверяется с `deploy/README.md`; tmpfs — размер из файла, оба пути под точкой
  монтирования; `logging` ни в одном оверлее, `cpus`/`pids_limit` в базовом файле. Раунд 8:
  правила map 411 и Н-25 проверяются таблицами входов через сами правила из файла, а не
  сравнением текста; покрытие зон — с наследованием от уровня server (зоны парка и адреса
  наследуются путями отказа); `proxy_buffering`, `proxy_max_temp_file_size` литералом с
  выводом размера tmpfs, `proxy_connect_timeout`/`proxy_send_timeout`, короткий
  `proxy_read_timeout` у отчётов, `proxy_intercept_errors` не включён; `worker_processes` равен
  квоте `cpus`; биты прав tmpfs проверяются по смыслу (проход для рабочего процесса, без чтения
  посторонними); пределы формы каждой модели тела литералами и отчёт равенством с ручной
  формулой (`tests/unit/test_body_shape.py`); гейт считает окно по обеим зонам и сумму всех
  классов в одном цикле; пара фикстур отчёта — пином `(2, 3)`; Н-25 —
  условие `$loggable` из двух map без учёта регистра; границы `QuotaGrant` и
  `Signals.new_conn_rate`; правило «последняя часть в ширине `bigint`»; `_is_type_checking`
  принимает только `TYPE_CHECKING` и `typing.TYPE_CHECKING`; любой сбой чтения тела на
  маршруте отчёта — 400 (тест с исключением из `receive`); фикстура продолжения интервала;
  срок закрытия — литерал 25 часов как Н-8 плюс предел интервала;
- один страж был удалён и восстановлен: во втором раунде переписывание хвоста
  `tests/unit/test_proxy_contract.py` стёрло `test_enrollment_is_served_only_by_its_own_server`
  (001.24); линза логики это заметила, страж восстановлен дословно из `HEAD`, в четвёртом
  раунде усилен положительными утверждениями и покрыт посадками (enrollment на агентском и на
  публичном server, удаление обоих блоков `/agent`). Запись здесь — чтобы отсутствие стража в
  diff раунда 2 не читалось как решение;
- стенд: репозиторий синхронизирован (`vm-sync.sh`), контейнеры пересозданы (`up -d`;
  `nginx.conf` — bind mount одного файла, после rsync прокси обязан быть пересоздан, а не
  перечитан: `nginx -s reload` в раунде 6 читал прежний inode — поймано по `nginx -T`), `nginx
  -t` успешен, потолки памяти — по cgroup (`memory.max`, `memory.swap.max` = 0), tmpfs
  временных файлов смонтирован (64 МиБ на `/var/cache/nginx`, подкаталоги nginx), `cpus` и
  `pids_limit` действуют, журналы ограничены. Раунд 7 (через nginx, `nginx -T` после
  пересоздания): chunked HTTP/1.1 на отчёте и heartbeat и поток HTTP/2 без content-length —
  411 с единым телом, то же на enrollment; heartbeat 204, состояние 409, отчёт 200 — не
  затронуты; путь отказа 64-КиБ тела из минимально коротких ключей ×200 — heartbeat p50 9 /
  p95 24 / max 40 мс `urt`, enrollment p95 17; залп 30 enrollment с одного адреса — 6 × 422 +
  24 × 429, пять медленных заливок с одного адреса — одна 429; зона парка 8 r/s — шесть
  сертификатов замкнутым циклом 20 с: 160 × 204, ровно 8,0 в секунду, задержка p50 0,74 с, ни
  одной строки «delaying» в error.log (уровень notice); Н-25 — семь форм пути с токеном
  (`%zz` — 400 до разбора URI, `//s/`, `/%73/`, `/x/../s/`, `/S/`, `/%53/`) ни разу не попали в
  access.log при живом журнале (map по `$request_uri` в раунде 6 пропускала 400, map с учётом
  регистра — `/S/`); RSS после проб — `api` 104 → 91 МиБ, `nginx` 9. Раунд 8 (пробы 22–23,
  после пересоздания nginx и перезагрузки api): tmpfs 0711 — рабочий процесс nginx проходит в
  `client_temp`, публичный POST 78 КБ — 405 приложения с временным файлом, `Permission denied`
  0 (в раунде 7 — 500); `DELETE`/`OPTIONS`/`POST` с `Transfer-Encoding: chunked` по HTTP/1.1 на
  отчёте и `DELETE` на enrollment — 411 единого формата (по HTTP/2 заголовка TE нет — `DELETE`
  с телом без длины читается в пределах предела и получает 405 приложения, это записано в
  README как равносильное заливке с известной длиной); Н-25 — сырые строки HTTP/1.1 с двойным
  пробелом, `M-SEARCH`, `//s/`, `/%73/`, absolute-form, `/x/../s/` и формы HTTP/2 — 0 строк с
  токеном, 9 строк `<unparsed>`; путь отказа после проверки формы — heartbeat 64 КБ из 8 543
  минимально коротких ключей ×200 — 422 `too_wide`, `urt` p50 1 / p95 2 / max 7 мс, enrollment
  ×60 — p95 2; худшее тело отчёта в пределах формы (60 КБ, 3 003 скобки, 9 006 запятых) ×100 —
  422, p50 14 / p95 37 / max 65; зона парка 15 r/s — 295 × 204 за 20 с, 14,8 в секунду, p50
  0,40 с; RSS `nginx` 6, `api` 119 МиБ. Через mTLS на 9443
  проверены (раунд 6): самое большое допустимое тело части (457 369 Б, байт в байт с
  `widest_report()`) одним потоком ×200 под heartbeat ~4/с — медиана 23 мс, p95 46, максимум
  105; сто пар частей, стартующих одновременно, — p95 второй из пары 69 мс (модель двух слотов
  — 100), heartbeat под парами — p95 5 мс, максимум 126; шесть сертификатов по ~2 части/с —
  p95 47 мс у принятых (сверх 2 r/s на сертификат — 429); путь отказа 64-КиБ heartbeat из
  лишних ключей ×200 — 422, `urt` p95 13 мс; то же на enrollment ×100 — p95 8 мс, залп 80 —
  21 × 422 + 59 × 429 единого формата с `Retry-After: 1`; худшее тело после проверки формы
  (500 + 2 500 пустых объектов) — 422 за 11 мс, 50 ошибок и остаток 8 950; чанки с
  расширениями по 4 КиБ и с ведущими нулями (12 МБ сырого потока на 3 КБ тела) — 200, два
  предупреждения о временном файле, файлы в tmpfs (36 КиБ занято при заливке 37 КБ тела),
  каталог пуст после; зона парка 15 r/s — шесть сертификатов по постоянным соединениям держатся
  на 15,0 r/s с задержкой 0,4 с (455 строк «delaying … agent_fleet_rate»); обрыв заливки прямо
  в api (100 Б из 5 000) — без трассировки и 500 в журнале; RSS `api` 120 МиБ после проб,
  `nginx` 19 → 28. Раунд 5: тело из 6 000 элементов ×200 — p95 88; 3 000 — p95 50 (два
  потока); 90 000 лишних ключей под 1 МиБ — 422 `too_wide` за 7 мс; 450 000 вложенных скобок —
  422 за 17 мс; 501 строка — 422 одной ошибкой `too_long`; без `parts_total` и с
  `parts_total: 0` — 422; негодный UTF-8 — 400 единого формата с русским текстом; грант с
  3 000 лишних ключей — 422 `extra_forbidden` с остатком; тело 1,17 МиБ — 413 от nginx; тело
  chunked по байту и потоком HTTP/2 без длины — 200, временных файлов nginx нет; вторая
  медленная заливка одним сертификатом — 429 в едином формате с `Retry-After: 1`; пять
  медленных заливок пятью сертификатами (выпущены на VM из dev CA) — 2 × 200 + 3 × 429; четыре
  потока HTTP/2 в одном соединении одним сертификатом — 1 × 200 + 3 × 429; два сертификата по
  12 мелких отчётов подряд и третий с медленной заливкой — третий 200; 40 heartbeat подряд —
  40 × 204 (очередь `agent_rate` замедляет, не отказывает); RSS `api` 108 → 132 МиБ после
  смешанной нагрузки, `nginx` 28 → 38; раунды 1–2: `node_mismatch` (403, `Retry-After: 3600`),
  401 без identity, 400 без сертификата, 404 на enrollment- и публичном server — см. отчёт;
- тесты: `tests/e2e/test_reports.py` (102 случая), `tests/unit/accounting/test_report_schema.py`
  (11), `tests/unit/accounting/test_stubs.py` (21), `tests/unit/jobs/test_handlers.py` (3),
  `tests/unit/test_validation_answer.py` (1), `tests/unit/test_body_shape.py` (7, раунд 8 и закрытие),
  `tests/unit/test_proxy_contract.py` (17 случаев, +11, переписан),
  `tests/contract/test_agent_v1.py`
  (33 случая по 21 фикстуре),
  `tests/e2e/test_agent_api.py` (инвентарь путей раздела — восемь; 400 и 411 у операций 001.28
  с телом, 413 и 503 у всех); общий строитель тел — `tests/_reports.py`.

Закрытие (2026-09-15, решение заказчика после роаста раунда 8 — пять REJECTED, счётчик
раундов исчерпан): задача закрыта как заглушка без девятого роаста; семь вынесенных на
эскалацию решений взяты исполнителем в рекомендованных вариантах под указанием «закрой
задачу» (бюджет свободного текста из `max_length` с доказательством алфавита шаблонных строк,
ассерт суммы классов снят, tmpfs не разделён — передано 001.66/001.75, 411 при отсутствии длины
у любого метода, кроме GET и HEAD, три случая удержания и списанная доля в `accepted_*`,
потолок возраста 48 ч); закрыто в дереве и проверено стендом (пробы 25–27): предел серии цифр
`bigint` у моделей без строк, Н-25 по пути до «?», 404 через `try_files` под пределами (return
404 даже с `error_page` пределов не давал — фаза REWRITE), таймаут заголовков 5 с,
`strict_content_type` явно, порядок ключей в эмуляторе map, документные находки роаста. Самая
дорогая остальная операция — результат команды с бюджетом текста: 4 мс p95, константа гейта
поднята, окно 116 ≤ 150, доля 64 ≤ 250. Подробности и свидетельства — отчёт, раздел «Закрытие
задачи по решению заказчика».

Найдено при 001.33 (заглушки) — записано в примечания задач-получателей («Найдено при 001.33,
решить здесь»): 001.19, 001.23, 001.25, 001.30, 001.34, 001.35, 001.36, 001.37, 001.38, 001.39,
001.50, 001.53, 001.56, 001.66, 001.68, 001.72, 001.73, 001.75, 001.77. Короткий перечень:

- 001.34: столбец «Учитывается трафик» §4.6 — принимать и не тарифицировать; принадлежность
  пользователей из `lines`/`online_ips` составу ноды; окно приёма (партиции `traffic_lines` —
  7 суток вперёд, 14 назад) как часть `rejected_time`; `held_anomaly` — 200 с продвижением
  `last_accepted_seq`, `rejected_time`/`interval_closed` — отказ 4xx без продвижения, номер
  пропускается; `CHECK (report_seq >= 1)`, колонка `parts_total`, индекс `(node_id,
  period_end)` и таблица `report_intervals` в схеме; правила частей: интервалы ноды монотонны и
  не перекрываются (и через смену эпохи), `period_end` не позже часов сервера с допуском,
  `parts_total` растёт только вверх, действует последнее принятое и применяется до проверки
  диапазона, порог §5.9 (полоса × длительность интервала) к нарастающей сумме принятых частей
  (суммы по направлениям) — тем самым по стенным часам, интервал закрывается завершением, частью
  следующего интервала или сроком `REPORT_PARTS_DEADLINE` (25 часов), часть к закрытому —
  `interval_closed`, открытых интервалов не больше 8 (сверх — `held_anomaly` и алерт), смен
  эпохи не больше 48 за скользящие сутки; длительность принятого интервала — метрика по ноде (алерт
  —
  001.68); пользователь в одной части и продолжение с ненулевыми счётчиками — отказ по
  контракту, не ошибкой вставки; ключ идемпотентности — по ноде identity; интервал через
  границу часа — к часу начала; нумерация после деления и после отказа; `counter_epoch`
  выбирает нода, смена эпохи для Control Plane неразличима — разрыв между `period_end` прежней
  и `period_start` новой (001.37);
- 001.77: шаги 4–7, `close_hour(conn, …)`, вызовы `on_report`/`evaluate` одним подключением,
  outbox `limits.check` в транзакции (§5.4, а не «после коммита»), переполнение `bigint` у
  произведения и накопления часа — граница по порогу полосы, а не по типу; счётчики
  интерфейса — только в первой части интервала, суммирование частей; `failed` от заглушки
  `limits.check` до 001.35;
- 001.35, 001.36, 001.38: батчевые сигнатуры с подключением; грант только ноде с полным потоком
  состава; пользователь в составе ноды; `issued_seq` монотонен в схеме; `limits.check` до
  001.35 отказывает `NonRetryableError`; `LimitsService(pool)` при вызывающем с `conn`;
  константа гранта — в домене, службы — из `deps.py`, типизация через `TYPE_CHECKING` или
  `Protocol`; частота `quota/request`; окно адресов собирается из нескольких частей;
- 001.37: перевести `SCHEDULE` на новые типы и снять `ensure_partitions` вместе со стражем;
  заглушки отказывают — до 001.37 их типы в очередь не ставить; срок хранения строк `jobs`;
  разрывы для закрытых без всех частей интервалов (по сроку и по следующему интервалу) и для
  смены эпохи с оценкой по полосе × длительность паузы между `period_end` и `period_start`;
  `traffic_gaps` — уникальность `(node_id, gap_start, reason)`, партиции по дню, срок 90 суток;
- 001.38: `DeviceLimitService(pool)` при вызывающем, имеющем только `conn`, — тот же вопрос,
  что у `LimitsService(pool)` (001.35); окно адресов из нескольких частей; вход лимита
  устройств (адреса и `last_seen`) целиком под контролем ноды — чему верить и потолок адресов
  на пользователя от одной ноды (строка §7.3);
- 001.39: `conn_stats[]` расширит `ReportIn` — предел `location` (1m), счётчики формы по
  байтам, гейт бюджета (константа 16,7 мкс измерена при отношении строк к адресам 1:5 — иной
  состав означает перемер) и страж самого большого тела (число байт) пересчитываются;
  `resale_signals(conn, …)` с окном 1…90 — `ValueError` вне окна защищает от ошибки
  программиста, маршрут панели объявляет окно `Query` (001.50); `volume_gb` — гигабайты 10⁹
  или переименование;
- 001.50: окно карточки признаков — `Query(ge=1, le=SIGNAL_WINDOW_MAX_DAYS)`, иначе `days` как
  есть — 500 вместо 422;
- 001.53, 001.56 (агент): деление на части с `parts_total` (растёт после деления отказанной
  части) и правила интервала (монотонные интервалы, закрытие завершением/следующим
  интервалом/25 часов, `interval_closed` без повтора), штатный интервал 60 с и дельта после
  перерыва не дольше часа, снятие счётчиков на границе часа UTC, канонические формы и строгие
  целые, `report_seq` с единицы, продолжение после отказа с `max(last_accepted_seq, номер
  отказанной) + 1` и нумерация после деления, поведение на 400/403/411/413/422/429 (429 —
  единый формат с `Retry-After`, повтор с джиттером, случайная фаза интервалов; 411 — чинить
  клиент), таймауты (отчёт — заливка при полосе ноды плюс 10 с, сквозное время законной части
  больше 200 мс Н-4; остальные операции — не меньше 30 с, long-poll — не меньше 60 с),
  `Content-Length` и `Content-Type`, фикстура `report-continuation.json`, сжатие тела — решить
  в 001.53;
- 001.68: метрика длительности принятого интервала по ноде и алерт на систематическое
  превышение штатных 60 с; алерт на OOM-kill контейнеров с потолком; алерты на удержание
  сверх потолка открытых интервалов и на смены эпохи сверх 48 за скользящие сутки; сток и срок
  хранения
  журналов nginx, адреса подписчиков в access.log (§16);
- 001.72: ограничение частоты в приложении стоит после разбора тела — пределы одновременности
  остаются на прокси; согласовать пороги приложения с зонами прокси (в том числе
  `quota/request`, `agent_fleet_rate` 15 r/s — путь отказа 2 мс после проверки формы, окно
  разбора по обеим зонам и доля цикла в гейте); `error_page 429` прокси не перехватывает 429
  апстрима
  (`proxy_intercept_errors` выключен) — 429 приложения сохранят своё тело; enrollment-server:
  пределы по адресу (1 r/s, 4 соединения) и общий потолок 2 r/s стоят, хеш токена и
  распределённый источник и NAT — здесь; зона парка как рычаг двух сертификатов; публичный server
  без пределов; `worker_connections` 1024 при росте парка; остаточный риск медленной заливки;
- 001.66: политика перезапуска после OOM (`restart:`) для обоих контейнеров с потолком,
  пределы, таймауты, шифры и порт 80 публичного server; промышленный профиль сохраняет tmpfs
  `/var/cache/nginx`, `cpus`/`pids_limit` и предел журналов базового файла и не заводит файлов
  Compose вне команды запуска (страж сверяет её с `deploy/README.md`);
- 001.19: `close_hour(conn, node_id, at)` вызывается на подключении смены коэффициента;
- 001.23: заменить `billable` и ассерт заглушки;
- 001.25: `ssl_crl` на агентском server — до него отозванная identity держит слоты прокси;
  профиль листового сертификата (`basicConstraints CA:FALSE`, `extendedKeyUsage clientAuth`) —
  `ssl_verify_depth 1` уже отвергает цепочки через лист, профиль закрепляет это у CA; перемер
  стоимости пути отказа enrollment после настоящей подписи CSR (константа гейта 2 мс измерена
  на заглушке с проверкой формы по байтам);
- 001.73: стоимость элемента (сквозной `urt`, не «разбор») и резерв транзакции на целевой VPS
  вместо констант стенда, замер с занятыми обоими слотами (под пределами прокси для этого
  нужно больше 20 сертификатов), потолки памяти, процессора и процессов обоих контейнеров под
  нагрузкой парка, p95 всех операций под парком, пропускная способность 16…27 частей в секунду
  по ширине части и полосе (цикл на насыщении — 20),
  429 зоны парка под залпом парка §5.7, пул и keepalive, `--limit-concurrency`;
- 001.75: long-poll и разбор отчётов делят цикл событий процесса — явная передача: отдельный
  процесс для приёма отчётов или удержания, если p95 остальных операций под парком выйдет за
  Н-4 (числа стенда — в задаче: две части одновременно — до 100 мс по модели, замер пар —
  69 мс, плюс операции обеих зон по 2 мс за окно; heartbeat под парами — p95 5 мс, максимум
  126; ответ апстрима прокси читает целиком и не держит корутину api).
- Раунд 8, короткий перечень новых передач: 001.34 — удержанная часть открывает свой интервал,
  якорь первого интервала (identity, сутки Н-8), потолок возраста, суммы по направлениям,
  `interval_closed` в `report_status`, индексы приёма, списание удержанного в пределах потолка
  канала; 001.35/001.30 — `suspended` по повторным удержаниям; 001.37 — PK с ключом
  партиционирования у `traffic_gaps`/`reconciliation_runs`, закрытие интервалов по возрасту в
  расписании; 001.38 — удержанные части вне окна адресов; 001.53/001.56 — обработка перезапуска
  Xray посередине интервала, один запрос отчёта в полёте, `too_wide`/`json_invalid`/503; 001.66
  — права tmpfs, `worker_processes`, файлы ответов на tmpfs, пределы публичного server; 001.68
  — алерт на каждое удержание, признак рычага зоны парка; 001.72 — два сертификата, NAT.

Зависимости: 001.28, 001.08. Приоритет: Critical. Оценка: 4 ч. Этап: 5 — учёт трафика и лимиты.
