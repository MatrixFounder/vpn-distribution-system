# Задача 001.36: Гранты квоты: выдача, замена, учёт расхода, ограничение Н-17

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-05 Исчерпание лимита и отзыв доступа

Требования RTM: R-26.

<!-- contract:goal -->

## Цель задачи

Реализовать правило гранта из `docs/architectures/interfaces.md` §5.2: `min(Н-17, остаток) / N`,
замена прежнего гранта, подтверждение расхода отчётами, включение `quota_grant_bytes` в поток
состава.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_limits.py` — суммарный объём грантов пользователя на N нодах ≤ Н-17; новый грант заменяет остаток

### Изменения в существующих файлах

#### Файл: `control-plane/app/accounting/quota.py`

- `class QuotaService`: `grant(user_id, node_id) -> int`; `on_report(user_id, node_id, billable)` → `consumed_bytes`; `settings.quota_grant_default` (ОВ-A7)

#### Файл: `control-plane/app/agent_api/reports.py`

- `POST /agent/v1/quota/request` → `QuotaService.grant`

#### Файл: `control-plane/app/domain/composition.py`

- `quota_grant_bytes` в строке состояния из последнего непогашенного гранта

### Интеграция компонентов

Локальное применение гранта на ноде — 001.57.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Сумма грантов ограничена
   - Входные данные: пользователь на 3 нодах; запросы грантов
   - Ожидаемый результат: сумма непогашенных ≤ Н-17 в единицах списания (AC-09)
2. **TC-E2E-02:** Новый грант заменяет
   - Входные данные: повторный запрос при остатке
   - Ожидаемый результат: прежний `superseded_at`; выдан один новый

### Модульные тесты

1. **TC-UNIT-01:** Размер гранта
   - Проверяемая функция: `app/accounting/quota.py::grant_size`
   - Входные данные: остаток 1 ГБ, N = 4, Н-17 = 3 ГБ
   - Ожидаемый результат: 256 МБ

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_limits.py -k quota tests/unit/accounting/test_quota.py`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Н-17в: размер гранта не превышает Н-17
- [ ] `consumed_bytes` подтверждается отчётами

## Примечания

Размер по умолчанию и шаг — ОВ-A7.

Найдено при 001.33 (заглушки), решить здесь:

- `QuotaService.grant(node, user_id, consumed_bytes) -> QuotaGrant` (отклонение от
  `grant(user_id, node_id) -> int`): нода целиком — вентиль §4.6 применяет служба: грант
  выдаётся только ноде с полным потоком состава (`provisioning`, `disabled`, `suspended` — 403
  `grant_unavailable`, `Retry-After: 300`, фикстура `quota-request-refused.json`); `issued_seq`
  живёт в `quota_grants` и известен только службе; `consumed_bytes` — самоотчёт, назначение
  (сигнал расхождения с подтверждённым расходом) решить здесь;
- пользователь обязан присутствовать в составе запрашивающей ноды — иначе любая нода
  выпрашивает гранты чужим пользователям;
- `on_report(conn, node_id, billable: Mapping[user_id, int])` — один вызов на отчёт в
  транзакции приёма (001.77);
- схема: монотонность `issued_seq` на пару «пользователь × нода» (`CHECK`/уникальность) —
  сегодня правило живёт только в README контракта;
- `STUB_QUOTA_GRANT_BYTES` живёт в домене (`domain/composition.py`), служба грантов импортирует
  его оттуда — домен от учёта не зависит (правило слоёв в `accounting/.AGENTS.md`): заменяя
  заглушку, дать потоку состава настоящий грант через `QuotaService` из графа зависимостей
  (`agent_api/deps.py`), а не через импорт константы и не импортом учёта из домена;
- `QuotaService` приходит в `AccountingService` из `deps.get_accounting_service`: подменять и
  расширять фабрику, а не конструктор службы приёма;
- типизация без ребра домен → учёт: страж правила слоёв разбирает импорты и **не считает**
  ветку `if TYPE_CHECKING:` — `CompositionService` может аннотировать `QuotaService` так; либо
  объявить в домене `Protocol` (например, `QuotaSource.grant(...)`), которому `QuotaService`
  удовлетворяет структурно. `Any` на денежном пути не допускается;
- `POST /agent/v1/quota/request` после этой задачи пишет в `quota_grants`: частота по
  сертификату на прокси — 5 запросов в секунду с очередью 20 (`agent_rate`) и 15 в секунду на
  весь парк (`agent_fleet_rate`, очередь 200), предел по identity в приложении — 001.72;
  согласовать порог выдачи (не чаще, чем нода может законно израсходовать грант).

Зависимости: 001.35. Приоритет: High. Оценка: 3 ч. Этап: 5 — учёт трафика и лимиты.
