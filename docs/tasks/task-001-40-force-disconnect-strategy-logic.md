# Задача 001.40: Стратегия принудительного разрыва: выбор кандидата, указание ноде

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-05 Исчерпание лимита и отзыв доступа

Требования RTM: R-25.

<!-- contract:goal -->

## Цель задачи

Реализовать выбор механизма разрыва по `settings.disconnect_strategy` (ОВ-22) и передачу указания
ноде вместе с отзывом; три кандидата §4.11 источника поддерживаются на стороне агента (001.60).

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_limits.py` — строка отзыва несёт стратегию

### Изменения в существующих файлах

#### Файл: `control-plane/app/domain/composition.py`

- поле `disconnect: readd | route_block | restart_inbound` в строке отзыва потока состава

#### Файл: `control-plane/app/api/admin/settings.py`

- настройка стратегии; по умолчанию `readd` до результата PoC-1а

### Интеграция компонентов

Значение Н-17а измеряется на стенде (AC-08) после выбора стратегии по PoC-1а.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Стратегия передаётся
   - Входные данные: отзыв при `route_block`
   - Ожидаемый результат: строка состояния содержит `disconnect = route_block`

### Модульные тесты

- Не требуются: задача не содержит логики сверх заглушек или конфигурации.

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_limits.py -k disconnect`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Стратегия настраивается без выпуска
- [ ] ОВ-22 закрывается изменением настройки

## Примечания

Н-17 предварительно до PoC-1а.

Зависимости: 001.35, 001.31. Приоритет: High. Оценка: 2 ч. Этап: 5 — учёт трафика и лимиты.
