# Задача 001.37: Сверки, суточная агрегация, партиции и сроки хранения

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-04 Учёт трафика и списание с коэффициентом
- UC-10 Восстановление Control Plane из резервной копии

Требования RTM: R-21, R-22, R-48.

<!-- contract:goal -->

## Цель задачи

Реализовать три сверки §5.9 источника, суточную агрегацию `traffic_daily`, создание и удаление
партиций и удаление данных по срокам §4.5 архитектуры и §16.2 источника.

<!-- contract:changes -->

## Описание изменений

### Изменения в существующих файлах

#### Файл: `control-plane/app/accounting/reconcile.py`

- `arithmetic(day)`, `cross_source(node_id, day, tolerance_pct)` (ОВ-24), `continuity(node_id)` — запись `reconciliation_runs`, `traffic_gaps`, алерты

#### Файл: `control-plane/app/accounting/aggregate.py`

- `aggregate_day(day)`: `traffic_daily` из `traffic_hourly`; повторный запуск идемпотентен

#### Файл: `control-plane/app/jobs/handlers/maintenance.py`

- `partitions.ensure` (7 суток вперёд), `partitions.drop_expired`, `retention.purge` (таблицы без партиций по §4.5) — вызовы функций `SECURITY DEFINER`

#### Файл: `control-plane/app/jobs/scheduler.py`

- расписание: агрегация 00:10 UTC, сверки 00:30 UTC, партиции 01:00 UTC, ретенция 02:00 UTC

### Интеграция компонентов

Функции обслуживания партиций созданы в 001.08; здесь — вызов и расписание.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Арифметическая сверка
   - Входные данные: отчёты за сутки и балансы
   - Ожидаемый результат: расхождение 0 %; при подмене баланса > 0,5 % — алерт (Н-18)
2. **TC-E2E-02:** Непрерывность
   - Входные данные: пропуск `report_seq`
   - Ожидаемый результат: `traffic_gaps` строка; алерт
3. **TC-E2E-03:** Ретенция
   - Входные данные: строки старше срока
   - Ожидаемый результат: удалены функцией владельца; `app_rw` не выполнял `DELETE`

### Модульные тесты

1. **TC-UNIT-01:** Идемпотентность агрегации
   - Проверяемая функция: `app/accounting/aggregate.py::aggregate_day`
   - Входные данные: двойной запуск
   - Ожидаемый результат: `traffic_daily` без дублей

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_reconcile.py tests/unit/accounting/test_aggregate.py`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Три сверки выполняются по расписанию
- [ ] Н-21, Н-22, §16.2: сроки хранения применяются автоматически
- [ ] AC-39 сохраняется: удаление `audit_log` по сроку — только функцией ретенции

## Примечания

Ограничения и допущения — `docs/idea.md` §9; архитектура — `docs/ARCHITECTURE.md`.

Зависимости: 001.09, 001.77. Приоритет: High. Оценка: 4 ч. Этап: 5 — учёт трафика и лимиты.
