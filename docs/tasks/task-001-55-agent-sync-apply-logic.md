# Задача 001.55: Node Agent: цикл long-poll, курсоры, снапшот, `resync_required`, `state_generation`

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-01 Ввод ноды в эксплуатацию
- UC-05 Исчерпание лимита и отзыв доступа
- UC-11 Обновление парка нод

Требования RTM: R-04.

<!-- contract:goal -->

## Цель задачи

Реализовать цикл синхронизации: long-poll с курсорами, обработку 409 и `resync_required` (полный
снапшот), сохранение курсоров в Store, `Ack` после применения, backoff при ошибках.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `node-agent/internal/sync/loop_test.go` — тест против тестового Control Plane

### Изменения в существующих файлах

#### Файл: `node-agent/internal/sync/loop.go`

- `Run(ctx, deps)`: `State → apply → Ack`; 409 → `full=1`; `resync_required` → `full=1`; курсоры в Store

### Интеграция компонентов

Финальное чтение счётчиков перед перезапуском — 001.56; правила — 001.58.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Снапшот по поколению
   - Входные данные: ответ 409
   - Ожидаемый результат: следующий запрос `full=1`; курсоры сброшены
2. **TC-E2E-02:** Курсоры сохраняются
   - Входные данные: перезапуск агента
   - Ожидаемый результат: запрос с сохранёнными курсорами

### Модульные тесты

1. **TC-UNIT-01:** Обработка ответа без изменений
   - Проверяемая функция: `internal/sync::handleState`
   - Входные данные: 204
   - Ожидаемый результат: курсоры без изменений; немедленный следующий запрос

### Регрессионные тесты

- Команда: `cd node-agent && go test ./internal/sync/...`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Курсоры переживают перезапуск агента
- [ ] Снапшот запрашивается при 409 и `resync_required`

## Примечания

`tdd-strict`.

Зависимости: 001.54, 001.29. Приоритет: Critical. Оценка: 4 ч. Этап: 9 — Node Agent (Go).
