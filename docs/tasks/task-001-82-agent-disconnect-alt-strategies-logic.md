# Задача 001.82: Node Agent: стратегии разрыва `route_block` и `restart_inbound`

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-05 Исчерпание лимита и отзыв доступа

Требования RTM: R-25.

<!-- contract:goal -->

## Цель задачи

Реализовать стратегии `route_block` (правило `RoutingService` по адресам пользователя онлайн на
`blackhole`) и `restart_inbound` (перезапуск inbound) за интерфейсом `Strategy`, с измерением
времени разрыва для PoC-1а.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `node-agent/internal/disconnect/strategies_test.go` — каждая против mock Xray

### Изменения в существующих файлах

#### Файл: `node-agent/internal/disconnect/strategies.go`

- две стратегии

### Интеграция компонентов

Выбор — поле `disconnect` (001.40); измерение на стенде — 001.73.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** `route_block`
   - Входные данные: строка отзыва с `route_block`
   - Ожидаемый результат: правила добавлены для всех адресов пользователя онлайн

### Модульные тесты

1. **TC-UNIT-01:** Выбор стратегии
   - Проверяемая функция: `internal/disconnect::ByName`
   - Входные данные: `route_block`, неизвестное
   - Ожидаемый результат: реализация / ошибка

### Регрессионные тесты

- Команда: `cd node-agent && go test ./internal/disconnect/...`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Обе стратегии реализованы; время разрыва измеряется

## Примечания

Итоговый выбор — ОВ-22; невыбранные стратегии удаляются после 001.73.

Зависимости: 001.60, 001.58. Приоритет: High. Оценка: 3 ч. Этап: 9 — Node Agent (Go).
