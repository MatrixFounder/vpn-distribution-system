# Задача 001.44: Динамический состав серверов: фильтры, статусы, страны, `users_seq`

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-03 Получение подписки клиентом
- UC-07 Отказ и возврат ноды
- UC-06 Продление и смена тарифа

Требования RTM: R-16.

<!-- contract:goal -->

## Цель задачи

Реализовать `compose` по фильтру R-16: состояние подписки, тариф, группы доступа, протоколы, статус
ноды по матрице §4.6, доступность по стране, подтверждённый `users_seq` ноды.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_subscription.py` — исключение `offline` в сроки Н-15; возврат; смена тарифа меняет состав в сроки Н-13 (UC-06 A2)

### Изменения в существующих файлах

#### Файл: `control-plane/app/subscription/composer.py`

- SQL с соединением `plans → plan_access_groups → node_access_groups → nodes → inbounds`; условия: `nodes.status IN (active, degraded)`, `inbounds.enabled`, `plan_protocols`, `node_country_availability`, `applied_users_seq >= desired_users_seq` для строк пользователя

### Интеграция компонентов

`EXPLAIN` запроса прикладывается к задаче (§8.3 архитектуры).

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Offline исключается
   - Входные данные: нода → `offline`
   - Ожидаемый результат: следующий ответ без ноды; после `active` — с нодой (AC «Автообновление»)
2. **TC-E2E-02:** Неподтверждённый состав
   - Входные данные: нода `active`, `applied_users_seq < desired` для пользователя
   - Ожидаемый результат: нода не выдаётся до подтверждения (UC-01 шаг 11)

### Модульные тесты

1. **TC-UNIT-01:** Фильтр протоколов
   - Проверяемая функция: `composer.py::compose`
   - Входные данные: тариф только с `vless_raw_vision`
   - Ожидаемый результат: записи других профилей отсутствуют

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_subscription.py -k compose`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Шесть условий фильтра R-16 реализованы
- [ ] План запроса без последовательного сканирования `traffic_*`

## Примечания

Ограничения и допущения — `docs/idea.md` §9; архитектура — `docs/ARCHITECTURE.md`.

Зависимости: 001.43, 001.30. Приоритет: High. Оценка: 3 ч. Этап: 6 — subscription-эндпоинт и логика
кабинета.
