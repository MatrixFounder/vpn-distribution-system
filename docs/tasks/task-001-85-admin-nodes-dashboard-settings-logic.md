# Задача 001.85: API панели: карточка ноды с метриками, дашборд, настройки

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-07 Отказ и возврат ноды
- UC-09 Управление тарифами, группами и кодами

Требования RTM: R-34.

<!-- contract:goal -->

## Цель задачи

Реализовать карточку ноды (статус, метрики за 24 часа, адреса онлайн, соединения, `desired/applied`,
inbound с `error_state`), дашборд §4.15 и настройки с валидацией (домены ≥ 2) и кешем 30 с.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_admin.py` — карточка ноды, дашборд, настройки

### Изменения в существующих файлах

#### Файл: `control-plane/app/api/admin/nodes.py`

- карточка

#### Файл: `control-plane/app/api/admin/dashboard.py`

- счётчики

#### Файл: `control-plane/app/api/admin/settings.py`

- валидация; Audit Log; кеш

### Интеграция компонентов

Все действия проходят `require_permission` и `audit.record`.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Один домен подписки отклонён
   - Входные данные: `PATCH /admin/settings` с одним доменом
   - Ожидаемый результат: 422

### Модульные тесты

1. **TC-UNIT-01:** Валидация доменов
   - Проверяемая функция: `admin/settings.py::validate_domains`
   - Входные данные: один домен
   - Ожидаемый результат: ошибка

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_admin.py -k 'node or dashboard or settings'`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Разделы Nodes, Dashboard, Settings обслуживаются
- [ ] Изменение настроек в Audit Log

## Примечания

Ограничения и допущения — `docs/idea.md` §9; архитектура — `docs/ARCHITECTURE.md`.

Зависимости: 001.49, 001.30, 001.48. Приоритет: High. Оценка: 4 ч. Этап: 7 — администрирование,
RBAC, аудит, поддержка.
