# Задача 001.86: Node Agent: nftables — лимит соединений с адреса, egress-фильтры, conntrack

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-08 Ограничение числа адресов и перепродажа доступа

Требования RTM: R-28, R-29.

<!-- contract:goal -->

## Цель задачи

Реализовать применение набора nftables (порты 25/587, частные сети, метаданные, частота новых
соединений, лимит одновременных соединений с адреса Н-29) с параметрами из потока конфигурации и
сбор счётчиков conntrack для признаков перепродажи.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `node-agent/internal/guard/guard_test.go` — набор содержит пять правил §16.6

### Изменения в существующих файлах

#### Файл: `node-agent/internal/guard/nftables.go`

- `Render`, `Load`, проверка `nft list ruleset`

#### Файл: `node-agent/internal/guard/conntrack.go`

- `Stats()` → поле `conn_stats[]` отчёта

### Интеграция компонентов

Значения лимитов — поток конфигурации; отчёт — 001.56.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Egress
   - Входные данные: соединение с ноды на порт 25 и на `169.254.169.254`
   - Ожидаемый результат: отклонено (AC-26)
2. **TC-E2E-02:** Лимит соединений
   - Входные данные: соединения сверх `max_conn_per_ip`
   - Ожидаемый результат: отклонены (AC-11)

### Модульные тесты

1. **TC-UNIT-01:** Генерация набора
   - Проверяемая функция: `internal/guard::renderRuleset`
   - Входные данные: параметры
   - Ожидаемый результат: содержит `tcp dport {25, 587} drop`, `ip daddr 169.254.169.254 drop`, `ct count over N`

### Регрессионные тесты

- Команда: `cd node-agent && go test ./internal/guard/... -run Nft`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] AC-11, AC-26, AC-40 выполнены

## Примечания

Проверка на стенде с реальной нодой (§17.1).

Зависимости: 001.58. Приоритет: High. Оценка: 4 ч. Этап: 9 — Node Agent (Go).
