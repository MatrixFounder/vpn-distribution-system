# Задача 001.52: Уведомления: события, подавление повторов, язык получателя

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-05 Исчерпание лимита и отзыв доступа
- UC-07 Отказ и возврат ноды
- UC-15 Восстановление пароля

Требования RTM: R-39, R-40.

<!-- contract:goal -->

## Цель задачи

Реализовать `emit` с `dedup_key` за период подписки, семь пользовательских и пять операторских
событий §4.17 источника, группировку операторских по ноде и типу, окно обслуживания, постановку
задач доставки на языке профиля.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_notifications.py` — семь пользовательских → две задачи; пять операторских → webhook; подавление

### Изменения в существующих файлах

#### Файл: `control-plane/app/notifications/events.py`

- `dedup_key` = `{type}:{user_id}:{period_id}`; повтор в периоде не создаёт события; язык получателя из профиля

#### Файл: `control-plane/app/jobs/handlers/notifications.py`

- постановка `notify.email` и `notify.webhook`; группировка операторских; окно обслуживания из `settings`

### Интеграция компонентов

Тест SMTP — локальный приёмник (`aiosmtpd`); webhook — локальный HTTP-сервер.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Повтор подавлен
   - Входные данные: дважды `traffic.80` в периоде
   - Ожидаемый результат: одно событие
2. **TC-E2E-02:** Операторское событие
   - Входные данные: `node.offline` дважды за минуту по одной ноде
   - Ожидаемый результат: одна группа доставки

### Модульные тесты

1. **TC-UNIT-01:** Ключ подавления
   - Проверяемая функция: `notifications/events.py::dedup_key`
   - Входные данные: `traffic.80`, пользователь, период
   - Ожидаемый результат: детерминированная строка

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_notifications.py tests/unit/notifications`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] 12 типов порождают задачи по таблице §4.17
- [ ] Повторы подавляются в пределах периода

## Примечания

Домен писем и записи SPF/DKIM/DMARC — 001.66.

Зависимости: 001.51, 001.65. Приоритет: High. Оценка: 3 ч. Этап: 8 — уведомления и почта.
