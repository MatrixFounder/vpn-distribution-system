# Задача 001.60: Node Agent: стратегия разрыва `readd` (значение по умолчанию)

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-05 Исчерпание лимита и отзыв доступа

Требования RTM: R-25.

<!-- contract:goal -->

## Цель задачи

Реализовать интерфейс `Strategy`, выбор по полю `disconnect` строки отзыва и стратегию `readd`
(удаление и повторное добавление пользователя) с измерением времени разрыва; после закрытия ОВ-22
невыбранные стратегии удаляются (001.73).

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `node-agent/internal/disconnect/strategies_test.go` — `readd` против mock Xray

### Изменения в существующих файлах

#### Файл: `node-agent/internal/disconnect/strategies.go`

- `ByName`; `readd`; метрика `disconnect_seconds`

### Интеграция компонентов

Вызывается при `RemoveUser` для состояний, снимающих доступ (001.55), и при исчерпании гранта
(001.57).

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Стратегия по умолчанию
   - Входные данные: строка отзыва без поля `disconnect`
   - Ожидаемый результат: применён `readd`; метрика записана

### Модульные тесты

1. **TC-UNIT-01:** Выбор стратегии
   - Проверяемая функция: `internal/disconnect::ByName`
   - Входные данные: `readd`, неизвестное
   - Ожидаемый результат: реализация / ошибка

### Регрессионные тесты

- Команда: `cd node-agent && go test ./internal/disconnect/...`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] `readd` реализован; время разрыва измеряется и экспортируется (Н-17а)

## Примечания

Итоговый выбор — ОВ-22 по PoC-1а.

Зависимости: 001.40, 001.80. Приоритет: High. Оценка: 3 ч. Этап: 9 — Node Agent (Go).
