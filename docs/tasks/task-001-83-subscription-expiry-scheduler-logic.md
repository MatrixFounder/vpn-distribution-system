# Задача 001.83: Служба подписок: истечение по расписанию и уведомление «истекает»

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-06 Продление и смена тарифа

Требования RTM: R-24, R-31.

<!-- contract:goal -->

## Цель задачи

Реализовать `expire` планировщиком (раз в минуту) с переводом в `expired` и публикацией отзыва в
поток состава, а также уведомление «Подписка истекает» за срок из `settings`.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_subscriptions.py` — истечение: `expired` не позднее минуты; событие «Подписка истекла»

### Изменения в существующих файлах

#### Файл: `control-plane/app/domain/subscriptions.py`

- `expire(user_id)`; `notify_expiring()`

#### Файл: `control-plane/app/jobs/scheduler.py`

- `subscription.expire` раз в минуту; `subscription.notify_expiring` раз в час

### Интеграция компонентов

События — 001.52.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Истечение по расписанию
   - Входные данные: `period_end` в прошлом; тик планировщика
   - Ожидаемый результат: состояние `expired`; событие в `events`

### Модульные тесты

1. **TC-UNIT-01:** Выбор истекающих
   - Проверяемая функция: `app/domain/subscriptions.py::expiring_within`
   - Входные данные: срок 3 дня
   - Ожидаемый результат: подписки с `period_end` в окне

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_subscriptions.py -k expire`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Истечение переводит в `expired` не позднее минуты после `period_end`
- [ ] Уведомление однократно за период

## Примечания

Ограничения и допущения — `docs/idea.md` §9; архитектура — `docs/ARCHITECTURE.md`.

Зависимости: 001.22. Приоритет: High. Оценка: 2 ч. Этап: 3 — тарифы, подписки, коды.
