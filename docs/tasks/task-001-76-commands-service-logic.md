# Задача 001.76: Служба команд: выдача в ответе состояния, идемпотентность, TTL, статусы

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-11 Обновление парка нод
- UC-12 Компрометация ноды

Требования RTM: R-04.

<!-- contract:goal -->

## Цель задачи

Реализовать `CommandService`: выдача команд в ответе long-poll по матрице §4.6, `expires_at`, повтор
по `command_id` без повторного исполнения, статусы `issued → delivered → applied|failed|expired`,
приём результата.

<!-- contract:changes -->

## Описание изменений

### Новые файлы

- `control-plane/tests/e2e/test_agent_api.py` — команды: повтор, TTL, статусы

### Изменения в существующих файлах

#### Файл: `control-plane/app/domain/commands.py`

- реализация всех методов; порядок по `issued_at`

#### Файл: `control-plane/app/agent_api/commands.py`

- приём результата; проверка принадлежности команды ноде из identity

### Интеграция компонентов

Полезные нагрузки конкретных команд — 001.31.

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Повтор не исполняется
   - Входные данные: команда доставлена дважды
   - Ожидаемый результат: `delivered` один раз; результат один

### Модульные тесты

1. **TC-UNIT-01:** Порядок выдачи
   - Проверяемая функция: `app/domain/commands.py::pending`
   - Входные данные: три команды с разным `issued_at`
   - Ожидаемый результат: по возрастанию

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/e2e/test_agent_api.py -k command`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Повторная доставка не исполняется повторно
- [ ] Команды не выдаются в статусах по матрице §4.6

## Примечания

Ограничения и допущения — `docs/idea.md` §9; архитектура — `docs/ARCHITECTURE.md`.

Зависимости: 001.29. Приоритет: High. Оценка: 3 ч. Этап: 4 — парк нод и Node API.
