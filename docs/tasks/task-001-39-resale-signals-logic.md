# Задача 001.39: Признаки перепродажи доступа в карточке пользователя (приоритет S)

Тип задачи: `[LOGIC IMPLEMENTATION]`.

## Связь со сценариями
- UC-08 Ограничение числа адресов и перепродажа доступа

Требования RTM: R-29.

<!-- contract:goal -->

## Цель задачи

Собрать признаки §4.13 источника — объём, частота новых соединений, круглосуточность — из
`traffic_hourly` и метрик агента и отдать в карточку пользователя.

<!-- contract:changes -->

## Описание изменений

### Изменения в существующих файлах

#### Файл: `control-plane/app/accounting/signals.py`

- `resale_signals(user_id, days=7) -> Signals{volume_gb, new_conn_rate, active_hours_share}`

#### Файл: `control-plane/app/agent_api/reports.py`

- приём счётчиков conntrack по адресам из отчёта агента (поле `conn_stats[]`)

#### Файл: `control-plane/app/api/admin/users.py`

- поле `resale_signals` в карточке (001.50)

### Интеграция компонентов

Источник частоты соединений — агент (001.58, conntrack).

<!-- contract:tests -->

## Тестовые случаи

### Сквозные тесты

1. **TC-E2E-01:** Признаки отображаются
   - Входные данные: пользователь с круглосуточной активностью
   - Ожидаемый результат: `active_hours_share ≈ 1.0` в карточке

### Модульные тесты

1. **TC-UNIT-01:** Доля активных часов
   - Проверяемая функция: `app/accounting/signals.py::active_hours_share`
   - Входные данные: 24 строки часа из 168
   - Ожидаемый результат: ≈ 0,14

### Регрессионные тесты

- Команда: `cd control-plane && pytest tests/unit/accounting/test_signals.py`
- Полный набор: `make test` — существующие тесты проходят.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Три признака вычисляются; адреса назначения не собираются (§16.3)

## Примечания

Приоритет S.

Зависимости: 001.38, 001.86. Приоритет: Medium. Оценка: 2 ч. Этап: 5 — учёт трафика и лимиты.
