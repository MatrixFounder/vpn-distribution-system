# Directory: control-plane/

## Purpose
Control Plane (Python 3.14, FastAPI): пакет `app/` в трёх ролях процесса, миграции, тесты и
образ Docker. Карта пакета — `app/.AGENTS.md`.

## Files

### [pyproject.toml]
- Метаданные, зависимости `[project]` и `[dev]`, настройки ruff / mypy strict / pytest
  (`python_files = test_*.py` — единственный источник правил обнаружения для `make test-py`).

### [requirements-dev.lock]
- `pip freeze` venv разработки (`make setup`): рантайм + инструменты.

### [requirements.lock]
- Рантайм-зависимости образа — `pip freeze` чистого venv без `[dev]`; версии совпадают с
  `requirements-dev.lock`. Только готовые колёса (`--only-binary=:all:`).

### [Dockerfile]
- Образ C-01…C-03 на `python:3.14-slim`, пользователь `app` (uid 10001), зависимости из
  `requirements.lock`, приложение запускается из `/app` без установки пакета (разработка
  подменяет `/app/app` монтированием). `EXPOSE 8000`; `ENTRYPOINT docker-entrypoint.sh`.

### [docker-entrypoint.sh]
- Стартует от root: копирует секреты из `/run/host-secrets` в `/run/secrets` (владелец `app`,
  0400) и переключается на `app` через `setpriv`. Выбор роли по `APP_ROLE`: `api` →
  `uvicorn app.main:create_app --factory` (`APP_WORKERS`, `APP_RELOAD`; имена `UVICORN_*` не
  используются — их читает сам uvicorn); `worker-critical|worker-background` →
  `python -m app.jobs.worker --queue …`; `scheduler` → `python -m app.jobs.scheduler`.
  Аргументы командной строки выполняются как есть (`docker compose run api python -m
  app.cli …`). Перед `api` выполняются миграции (`MIGRATE_DSN`, `MIGRATE_PASSWORD_FILE`, 001.03);
  роль `api` поднимается с `--proxy-headers --forwarded-allow-ips '*'` (заголовки
  перезаписывает nginx — см. `tests/unit/test_proxy_contract.py`) и проходит healthcheck
  `/healthz`. Перед `setpriv` задаётся `HOME=/app USER=app`: с унаследованным `HOME=/root`
  asyncpg падал на поиске `~/.postgresql/` (001.11).

### [.dockerignore]
- Контекст сборки: `app/`, `migrations/`, `yoyo.ini`, `requirements.lock`, `docker-entrypoint.sh`.

### [yoyo.ini]
- Конфигурация CLI yoyo: `sources = migrations`, `database = %(MIGRATE_DSN)s` (для CLI — полный
  URL с паролем и схемой `postgresql+psycopg://`), `batch_mode = on`.

### [migrations/]
- `bootstrap/roles.sql` — роли `app_owner` (владелец базы, без входа), `app_rw`, `app_migrate`
  (член `app_owner`), `app_backup`, `app_audit_purge` (без входа; держатель DELETE на `audit_log`,
  `app_owner` — член без наследования, с SET ROLE); схема `control_plane` (владелец `app_owner`,
  `search_path` ролей приложения; `public` пуст); psql-переменные `:db`, `:rw`, `:mig`, `:bk`.
  Кластерные объекты: выполняется при инициализации кластера (`deploy/compose/postgres/initdb.d`)
  или в CI, не миграцией; на уже инициализированном томе — вручную тем же SQL (повторный запуск
  безопасен; так добавлена `app_audit_purge` в 001.09).
- `0001_extensions_roles_enums.sql` / `.rollback.sql` — `btree_gist`, `citext`, привилегии по
  умолчанию `app_rw`/`app_backup`, 22 перечисления §4.2. Каждая миграция начинается с
  `SET LOCAL ROLE app_owner; SET LOCAL search_path TO control_plane` — владелец объектов
  `app_owner`, всё в схеме `control_plane`; учёт yoyo ведёт `app_migrate` (его таблицы — там же).
- `040_schema_identity.sql` / `.rollback.sql` — `users` (citext email, uuid v7), `admin_users`,
  `admin_recovery_codes`, `email_tokens`, `auth_events` (партиции по суткам `ts`, `id` из
  `auth_events_id_seq`; партиции создаёт функция 001.08). `-- depends: 0001_…`.
- `050_schema_catalog.sql` / `.rollback.sql` — `plans`, `plan_protocols`, `access_groups`,
  `plan_access_groups`, `billing_groups`, `billing_group_multipliers` и
  `node_billing_assignments` (история коэффициентов: `CHECK` 0…10000 кратно 100, `EXCLUDE USING
  gist` по интервалам; FK `node_id → nodes` добавляет 060).
- `060_schema_nodes.sql` / `.rollback.sql` — `nodes` и 12 таблиц парка (§4.2.3): inbound с
  `UNIQUE (node_id, profile)`/`(node_id, port)`, identity, credentials, поток состава,
  команды, `node_metrics` (партиции по суткам), плюс отложенный FK
  `node_billing_assignments.node_id → nodes` (откат снимает его первым).
- `070_schema_subscriptions.sql` / `.rollback.sql` — периоды и состояние подписок, журнал
  баланса `balance_entries` (identity, без UPDATE/DELETE у `app_rw`, CHECK reason для
  adjustment), токены (частичный UNIQUE активного), журнал обращений (партиции по суткам),
  коды и активации, заглушки `orders`/`payments` (О-2).
- `080_schema_accounting.sql` / `.rollback.sql` — учёт трафика: отчёты (PK идемпотентности),
  `traffic_lines`/`traffic_hourly` (партиции по суткам; запреты UPDATE/DELETE и DELETE у
  `app_rw`; триггер «только рост» часа), агрегаты, разрывы, сверки, гранты, адреса; реестр
  `partition_policies` (§4.5) и функции `ensure_partitions(days_ahead)` /
  `drop_expired_partitions()` — SECURITY DEFINER от `app_owner`, сутки в UTC (`SET timezone`),
  вызывает планировщик под `app_rw` (001.11/001.37); новые партиции получают запреты §4.6 по
  реестру; откат 080 партиции чужих таблиц не трогает.
- `090_schema_ops.sql` / `.rollback.sql` — события, доставки (каскад к событию), очередь `jobs`
  (частичные индексы: выборка pending, уникальность ключа среди активных — R-46), `audit_log`
  append-only на привилегиях (UPDATE/DELETE/TRUNCATE отозваны у `app_rw` и у владельца; DELETE и
  SELECT(ts) только у `app_audit_purge`) плюс триггеры UPDATE/DELETE/TRUNCATE для всех ролей с
  пропуском только DELETE под `current_user = app_audit_purge`; `purge_audit_log()` — SECURITY
  DEFINER от `app_audit_purge` (создаётся под `SET LOCAL ROLE`, EXECUTE у PUBLIC отозван явно),
  12 месяцев, UTC; `settings` со строкой `state_generation` = 0.
- `100_settings_auth.sql` / `.rollback.sql` — умолчания настроек регистрации (001.14).
- `110_jobs_retry.sql` / `.rollback.sql` — `jobs.claimed_at`, `jobs.finished_at` и частичный
  индекс по `claimed_at` (001.74): времена выборки и завершения для метрики ожидания.
- `120_settings_email_tokens.sql` / `.rollback.sql` — умолчание `email_token_ttl_minutes` = 15
  (ОВ-25).
- Правила: пары apply/rollback, expand/contract (§4.6); перечисления только расширяются;
  нумерация — `0001` bootstrap-миграция, далее `NN0_…` по порядку применения с шагом 10
  (у первых миграций схемы номер совпадал с номером задачи: 040 = 001.04; с `100` номер
  задачи указывается в заголовке файла, а не в имени).

### [tests/]
- `conftest.py`: autouse `app_environment` — полное окружение `Settings` для ленивых пула и Redis
  (`APP_ROLE=api`, временный файл ключа шифрования, адреса стенда, если не заданы); фикстуры
  `pg_dsn` (`app_rw`), `redis_url` (стенд по `PG_DSN`/`REDIS_URL`),
  `app_client` — `httpx.AsyncClient` над `create_app()` через `ASGITransport`
  (`raise_app_exceptions=False`: 500 приходит ответом единого формата; lifespan транспорт не
  запускает).
- `e2e/test_skeleton.py`: задача 001.10 — старт без базы (закрытые порты, lifespan вручную),
  `/healthz`, `/openapi.json` (три префикса, версия, без `/docs`), формат ошибок 404/405/422/501/500,
  `/metrics`, ленивые пул и Redis против стенда (транзакция откатывает, закрытие идемпотентно);
  заглушка 501 — `subscription.get` (`me.profile` снята в 001.15, `admin.dashboard` ушла под
  сессию администратора в 001.18, `agent.state` — в 001.28); запросы к `/agent/v1/state` идут с
  заголовками ноды, иначе ответом был бы 426 или 401 до разбора курсоров.
- `e2e/test_me.py`: задача 001.15 на живом стенде (сессия — настоящая, через `_auth.auth_stand`;
  cookie `sid` ставится клиенту вручную — cookie `Secure` над `http://` хранилище cookie клиента
  httpx не вернёт) —
  ровно восемь операций `/me` в OpenAPI со схемами (`TrafficStats` = состав §4.12, `NodeTraffic`
  с `multiplier`, `CountryTraffic`, `SubscriptionOut.state` как enum, `Profile`, `OnboardingOut`),
  все восемь без сессии и с несуществующей → 401 без cookie, каждая мутация под сессией без
  `X-CSRF-Token` → 403 (параметризовано по `MUTATIONS`), сессия администратора → 401, профиль под
  идентификатором сессии и `PATCH` (422 на язык/пояс/лишнее поле), удаление и перевыпуск без
  `confirm=true` → 409 с предупреждением (TC-E2E-02), ссылки на двух доменах с одним токеном и
  новый токен после перевыпуска, `redeem` 200/422, состав статистики с числами примера §4.12
  (TC-E2E-01; `?period=` UUID иначе 422), `SubscriptionOut.state` сверяется с `enum_range`
  базы, мастер §17.6 с пятью клиентами §4.2 и закодированной ссылкой импорта, пустой
  `SUBSCRIPTION_DOMAINS` → 500, отказ Redis посреди запроса под живой сессией (подмена
  `SessionStore.get`) → 503 по всем операциям, у операций без тела нет `requestBody` и схемы
  `Settings` нет в OpenAPI. `e2e/_me.py` — `ME_OPERATIONS`, `REQUEST_BODY`, `MUTATIONS`, общие с
  fail-closed тестом.
- `unit/test_config.py`: TC-UNIT-01 001.10 — разбор окружения стенда, отсутствующий/пустой
  секрет → `SecretError`, неверная роль → `ValidationError`, пароль из файла в DSN (экранирование,
  IPv6, краевые пробелы сохранены); роль `api` без `SUBSCRIPTION_DOMAINS` → `ValidationError`,
  другим ролям домены не нужны (001.15).
- `e2e/test_jobs.py` (+ `e2e/_jobs.py`: пул под окружением стенда, типы `test-noop`/
  `test-boom`/`test-vanish` только в реестре тестов, свой ключ лидерства, уборка задач
  `test-jobs:`): задача 001.11 на живом стенде рядом с рабочими исполнителями — цикл enqueue →
  `run_once` → done и идемпотентность (дубль без порчи транзакции, ключ свободен после
  done/failed, running держит), независимость очередей, порядок `run_at, id`, failed при
  исключении обработчика, исчезнувшая строка не роняет исполнителя, неизвестный тип не
  выбирается, `run_at` в будущем, SKIP LOCKED при чужой блокировке, NOTIFY только при коммите,
  главный цикл: пробуждение по NOTIFY, восстановление после `pg_terminate_backend` слушателя и
  после ошибки выборки; планировщик: блокировка своего ключа, второй экземпляр не лидер, боевой
  ключ занят стендом, слоты `Periodic`, проверка интервала; `/metrics` с базой и без неё.
  001.74: дубль активного ключа — существующая задача (`created=False`), `boom` → повтор с
  задержкой ≥ базы, `test-fatal` → `failed`, задача в ожидании повтора держит ключ; TC-E2E-01
  — `max_attempts=3`, две растущие задержки (часы базы), `dead` с ERROR в журнале и рядом
  `control_plane_jobs{status="dead"}`; TC-E2E-02 — критичная задача выбрана по NOTIFY ≤ 1 с
  (`claimed_at − created_at`), ряды ожидания и бюджета; брошенная `running` отбирается после
  аренды с новой попыткой, свежая — нет, зомби-исполнитель не закрывает и не возвращает в очередь
  перехваченную задачу (`LookupError`); величина ожидания: не выбранная готовая задача даёт
  ≈ 30 с, после выборки — то же, строка в ожидании повтора не учитывается.
- `unit/jobs/test_queue.py`: статичность `CLAIM_SQL` (FOR UPDATE SKIP LOCKED, attempts при
  выборке), имена каналов, слоты и ключи `Periodic`, `SCHEDULE` содержит `ensure_partitions`
  раз в час с обработчиком в `HANDLERS`, `Enqueued` (id + `created`) — без базы.
- `unit/jobs/test_worker.py` (tdd-strict, 001.74): `backoff` 1/2/4 с с ограниченным джиттером,
  потолок и масштабирование базы, `decide` — retry / dead / failed (`NonRetryableError`).
- `unit/security/`: задача 001.12 — `test_passwords.py` (argon2id `m=65536,t=3,p=4`, соль,
  негодный хеш → ложь), `test_crypto.py` (обратимость, подделка nonce/текста/тега, усечение,
  чужой ключ и версия, валидация, ключ из файла настроек), `test_stubs.py` (`test_current_user_and_admin_active_permission_stubbed`: `current_user` и `current_admin` без cookie → 401, `require_permission` →
  `NotImplementedError`, ключи §5.12, `RateLimiter` fail-closed на закрытый порт).
- `e2e/test_auth.py` (+ `e2e/_auth.py`: `auth_stand()` — пул и Redis стенда, партиции
  `auth_events`, настройки по умолчанию, клиент со своим адресом источника 203.0.113.x, адреса
  `@test-auth.local`, токены из очереди `send_email`, уборка пользователей/событий/задач/сессий/
  счётчиков `rl:*`): задачи 001.13–001.14 на живом стенде — семь маршрутов `/auth` в OpenAPI со
  схемами, четырнадцать случаев валидации, UC-02 полный цикл (регистрация → письмо из очереди →
  подтверждение → вход с cookie `sid`/`csrf`, `auth_events` register/login, `aup_version`), A1
  дубль адреса в другом регистре → 409, две одновременные регистрации → 201 и 409, A5 порог по
  адресу источника (429 с `Retry-After`) не блокирует учётную запись с другого адреса, порог по
  учётной записи без провайдера CAPTCHA не запирает владельца (только отказы, сброс успехом) и с
  провайдером → 429 `captcha_required` до пароля, с провайдером пороги регистрации и входа по
  адресу источника предлагают CAPTCHA (без ответа 429 `captcha_required`, с верным — 201/вход),
  заблокированная/удалённая запись → 401 и без
  ссылки восстановления, A6/A7 режимы `closed` и `invite`, одноразовый домен и включённая
  CAPTCHA без провайдера, UC-15 (две сессии отозваны сменой пароля, ссылка одноразовая → 410, A1
  неизвестный адрес неотличим — тело, все заголовки кроме `Date`, медианы времени ответа по 12
  запросам < max(3 мс, 25 %), задача `reset_unknown`; стоимость `authenticate` для неизвестного
  адреса; A2 просроченная и чужая ссылка → 410; A3 порог по адресу почты с `Retry-After`; срок
  ссылок из `settings.email_token_ttl_minutes` — 15 мин по умолчанию и без строки, 2 и 240 мин по
  настройке, неверное значение → 500, миграция 120 сеет 15), выход
  без `X-CSRF-Token` → 403 и с ним → 204 с отзывом сессии, `authenticate → None` = 401 без
  cookie (подмена), TTL cookie и ключа сессии.
- `e2e/test_security_fail_closed.py`: TC-E2E-01 001.12 — без Redis `/api/v1/auth/login` и
  все семь маршрутов `/auth` и `/s/{token}` → 503 с `Retry-After` в едином формате без
  подробностей и без cookie, все восемь операций `/api/v1/me` и восемнадцать операций
  `/api/v1/admin` → 503 с `Retry-After` (сессии в Redis, 001.15/001.18), `/openapi.json` и
  `/healthz` не зависят; без базы (закрытый порт)
  `register`/`login` → 503, не 500 (001.14); с Redis стенда — вход без тела → 422 (валидация
  после fail-closed), `/s/{token}` → 501.
- `unit/domain/test_codes.py`: TC-UNIT-01 001.18 — 300 сгенерированных кодов: любая подмена
  одного символа ложь, перестановка соседних ложь (кроме разности индексов 16), регистр и
  дефисы не важны, мусор и символы вне алфавита ложь, `EXAMPLE_VALID`/`EXAMPLE_INVALID`,
  1000 кодов без повторов, нечётные веса; выдача через `secure_source()` (подмена на
  детерминированный источник — красная), не-строки → ложь, `csv_rows` экранирует и
  нейтрализует формулы.
- `e2e/test_catalog.py` (+ `e2e/_admin.py`: сессия администратора прямо в Redis, `ADMIN_OPERATIONS`
  с валидными телами, `MUTATIONS`): задача 001.18 — ровно восемнадцать операций `/admin` в
  OpenAPI, у каждой одно `x-permission` (R-35) — раздел равен сегменту пути, `read` у GET и
  `write` у мутаций, — схемы `PlanIn`/`CodeSpec` по колонкам data-model, только `text/csv` у
  экспорта, без `requestBody` у GET/DELETE, тег `admin` один; все операции без сессии,
  с несуществующей и с сессией пользователя → 401; каждая мутация без `X-CSRF-Token` → 403;
  TC-E2E-01 CRUD тарифа 201/200/200/204, `null` сбрасывает лимит и не трогает непереданные
  поля, `null` для NOT NULL → 422, семь случаев 422 на создании; группы и коэффициент (шаг 0.1,
  422 на 2.05/10.1/−0.1/abc); TC-E2E-02 код с верной суммой, партия, экспорт CSV с заголовком и
  кодами с верной суммой, `no-store`, вложение, CRLF, использования, 422 на негодную
  спецификацию; `redeem` отклоняет неверную сумму без базы; «выход везде» настоящего
  пользователя не трогает сессию администратора.
- `unit/domain/test_users.py`: TC-UNIT-01 001.14 — `normalize_email` (регистр, краевые
  пробелы), `hash_token` (SHA-256), `NoCaptchaProvider` отклоняет.
- `unit/security/test_ratelimit.py` (tdd-strict, Redis стенда): TC-UNIT-02 001.14 — шестой
  вызов при пороге 5/60 с ложь, ключи независимы и окно скользит, отклонённые попытки
  засчитываются, ключ истекает с окном, `peek` не засчитывает и не видит старые отметки, `reset`
  обнуляет, `retry_after` — срок до выхода самой старой отметки; `unit/security/test_sessions.py`
  (tdd-strict): создание (возвращает сессию с `csrf`)/чтение, отзыв одной и всех кроме текущей,
  скользящий TTL, множество субъекта с TTL и чисткой мёртвых членов.
- `unit/test_proxy_contract.py`: `X-Client-Fingerprint` ставит только прокси — агентский
  `server` перезаписывает его `$ssl_client_fingerprint`, публичный и enrollment обнуляют, а
  `$http_x_client_fingerprint` не встречается нигде: без этого ассерта одна строка конфигурации
  делает признак identity клиентским; `proxy_read_timeout` ≥ 60 с (удержание long-poll 001.75
  переживает прокси); предел тела агентского `server` — по операциям 001.28 (64 КБ; тело
  читается до отказа по версии и identity, а отчёты получат свой `location` в 001.33).
  Статический контракт nginx ↔ uvicorn (001.10) — во всех
  `server` с `proxy_pass $api` заголовки прокси перезаписываются `$remote_addr`/схемой, в файле
  нет `$proxy_add_x_forwarded_for`; обе ветки запуска uvicorn в `docker-entrypoint.sh` с
  `--proxy-headers --forwarded-allow-ips '*'`.
- `e2e/_cli.py`: `run_cli()`, `migration_count()`, `rollback_through()` (ровно столько
  откатов на шаг, сколько нужно снять миграцию и всё новее); `e2e/_db.py`: `existing_tables()`;
  `e2e/_db.py` также `insert_node()`, `insert_user()`, `insert_plan()`; `e2e/_spec.py`: ожидания по модели данных
  (`EXPECTED_ENUMS`, наборы таблиц групп).
- `unit/db/_introspect.py`: сверка таблицы со спецификацией по каталогам PostgreSQL (колонки,
  ограничения, индексы), права по `has_table_privilege` и `has_schema_privilege` — общий код
  стражей групп схемы.
- `unit/db/test_bootstrap_schema.py`: страж bootstrap-слоя — владелец схемы `control_plane`,
  права на неё (`USAGE` app_rw/app_backup/app_audit_purge, `USAGE, CREATE` app_migrate),
  `search_path` ролей, членство `app_owner` в `app_audit_purge` без наследования, `public` без
  объектов приложения.
- `unit/db/test_schema_catalog.py`: страж группы «тарифы, группы, коэффициенты» (§4.2.2, §4.4)
  и права `app_rw`/`app_backup` на семь таблиц.
- `unit/db/test_schema_accounting.py`: страж группы «учёт трафика» (§4.2.5, §4.4, §4.5, §4.6):
  11 таблиц с реестром, ключи партиционирования, реестр сроков, триггер, права (traffic_lines —
  SELECT, INSERT; traffic_hourly — без DELETE; реестр — SELECT), свойства функций обслуживания.
- `unit/db/test_schema_ops.py`: страж группы «события, задачи, аудит, настройки» (§4.2.6, §4.4):
  6 таблиц, частичные индексы `jobs`, два триггера `audit_log`, identity-последовательности и их
  права, права (`audit_log` — SELECT, INSERT у `app_rw`; владелец без UPDATE/DELETE/TRUNCATE;
  `app_audit_purge` — DELETE и SELECT только `ts`), свойства `purge_audit_log` (владелец
  `app_audit_purge`, EXECUTE только `app_rw`), строка `state_generation`.
- `unit/db/test_schema_subscriptions.py`: страж группы «подписки» (§4.2.4, §4.4): 9 таблиц,
  частичный UNIQUE, партиционирование журнала, права (для `balance_entries` — SELECT, INSERT)
  и две последовательности.
- `unit/db/test_schema_nodes.py`: страж группы «парк нод» (§4.2.3, §4.4): 13 таблиц, ключ
  партиционирования `node_metrics`, отложенный FK, точности numeric/char, права на таблицы и на
  `node_ip_history_id_seq`.
- `e2e/test_migrations.py`: TC-E2E-01 задачи 001.03 — `app.cli migrate`, `--rollback-all`,
  повторное применение; владельцы всех объектов `control_plane`, значения перечислений, записи
  `pg_default_acl`, `has_function_privilege` для `app_backup`/`app_rw`; статический страж
  `SET LOCAL ROLE` + `SET LOCAL search_path`; зависший замок yoyo → `migrate` код 1 с подсказкой,
  `migrate --break-lock` снимает (10 с таймаута замка); отметка без объектов (откат 090, затем
  `mark_migrations`) → `migrate` и `--rollback` код 65 с подсказкой и ничего не меняют,
  `--unmark` неизвестной миграции → 64, `--unmark 090_schema_ops` → повторное применение
  (WI-3). Без базы тесты падают с ошибкой подключения, не пропускаются.
- `e2e/test_schema_accounting.py`: задача 001.08 — откат ровно до 080 (функции и триггер уходят),
  ограничения и запреты §4.6 под `app_rw`, `ensure_partitions`/`drop_expired_partitions` под
  `app_rw` (владелец и права партиций, идемпотентность, граница срока хранения, сутки UTC под
  сессиями UTC-12/UTC+14, триггер на партиции); откат не трогает партиции и данные чужих таблиц;
  партиции теста удаляются.
- `e2e/test_schema_ops.py`: задача 001.09 — откат ровно до 090, ограничения и запреты §4.6 под
  `app_rw` (dedup, каскад доставок, идемпотентность задач среди активных, пара блокировки,
  `result`), `purge_audit_log` с границей ±1 с по `now()` базы; append-only под владельцем в три
  слоя (привилегии → `InsufficientPrivilege`; с возвращёнными правами и при подделке текста —
  `RaiseException`; роль очистки без UPDATE, DELETE под явным SET ROLE — объявленный остаток).
  `_db.py`: `owner_connection()` — psycopg под `app_migrate` + `SET ROLE app_owner`.
- `e2e/test_schema_subscriptions.py`: задача 001.07 — откат ровно до 070, ограничения §4.2.4 под
  `app_rw` (частичный UNIQUE токена, запрет UPDATE/DELETE на `balance_entries`, CHECK периодов и
  кодов, FK, каскад `subscriptions`), журнал обращений без партиции и во временной партиции.
- `e2e/test_schema_nodes.py`: задача 001.06 — откат ровно до 060 (снимает FK на nodes), ограничения
  §4.2.3 под `app_rw` (UNIQUE, CHECK, NOT NULL R-18, FK RESTRICT/CASCADE, enum), `node_metrics`
  без партиции и вставка во временную партицию 2000-01-01.
- `e2e/test_schema_catalog.py`: задача 001.05 — откат ровно до 050 и повтор, ограничения §4.2.2
  под `app_rw` (UNIQUE, CHECK коэффициента и границ, EXCLUDE на двух таблицах истории, FK, каскады).
- `e2e/test_schema_identity.py`: задача 001.04 — откат ровно до 040 и повтор, ограничения §4.2.1 под
  `app_rw` (UNIQUE citext, FK, enum, CASCADE, третий язык принимается), `auth_events`: без
  партиции строка отклоняется, вставка под `app_rw` во временную партицию 2000-01-01 (вне окна
  планировщика 001.08; создаёт и удаляет `app_owner`).
- `unit/db/test_schema_identity.py`: сверка каталогов с §4.2.1 (колонки, ограничения, индексы,
  ключ партиционирования, последовательность), права `app_rw`/`app_backup` на таблицы и на
  `auth_events_id_seq` по `has_table_privilege`/`has_sequence_privilege` (`information_schema`
  из сессии `app_rw` чужих прав не видит).

- `e2e/test_nodes.py`: задача 001.24 — контракт `/api/v1/admin/nodes/*` и `POST /agent/v1/enroll`
  в OpenAPI (наборы полей всех схем — равенством), TC-E2E-01 UC-01 на заглушках (запись →
  токен Н-24 → обмен → сверка отпечатка и версий → подтверждение → карточка), идентификатор из
  пути, валидация записи, enroll без сессии и CSRF, отказ CA → 422 `invalid_csr`, согласованность
  карточки и состояния. `unit/security/test_ca.py` — разбор PEM и отпечаток SHA-256 DER;
  `unit/domain/test_nodes.py` — источник случайности токена, срок Н-24, фиксированные значения,
  передача причины ручного статуса в домен.
- `e2e/test_agent_api.py`: задача 001.28 — контракт `/agent/v1` в OpenAPI (шесть путей, наборы
  полей, обязательность и перечисления равенством, объявленные коды ответов равенством, урок
  WI-6: `Settings` не публикуется, у операций без тела нет `requestBody`), TC-E2E-01 состояние
  ноды `active` и 426 без `X-Agent-Version`, TC-E2E-02 heartbeat и телеметрия → 204 без тела,
  оба признака identity обязательны и проверяются по форме на каждой операции (десять негодных
  наборов заголовков), признаки доходят до зависимости не перепутанными, 426 на восьми негодных
  версиях и 200 на семи годных, включая саму границу длины (границы литералами, константы
  закреплены отдельно), вентиль §4.6 по всем восьми статусам **в обеих формах запроса** —
  снапшот вентиль не обходит (параметризация по `STREAM_GATE` × delta/snapshot), ноде в
  `pending` не отвечает ни одна операция раздела, `provisioning` на применённой конфигурации
  получает 204 (§5.2: удержание до таймаута), снапшот против дельты, 409 на чужое поколение,
  валидация курсоров и тел с проверкой самих границ на приём, разобранное тело доходит до домена
  и отметка heartbeat идёт временем сервера (службы подменены записывающими — маршрут,
  выбросивший тело, отвечает тем же 204), служба команд берётся из графа зависимостей, `issue`
  считает срок от момента выдачи, тип задачи `composition.publish_user` в реестре обработчиков.
- `contract/test_agent_v1.py`: стадия «Контракты» §10.2 — запросы `contracts/agent_v1/*.json`
  воспроизводятся против приложения при объявленном фикстурой предусловии (`given.node_status`:
  статус живёт на сервере, а не в запросе), ответ сверяется со снятым; набор фикстур закреплён
  равенством (пропавший файл валит прогон, а не сокращает его молча), как и набор предусловий —
  вентиль §4.6 показан второй стороне обмена всеми четырьмя видами ответа; полным (`users.full`)
  объявлен ровно неурезанный снапшот, и `resync_required` с ним не сочетается; сроки команд в
  фикстурах не протухают; контрольная сумма пересчитывается по описанию из README, а не вызовом
  реализации; поток конфигурации проверяется на отсутствие credentials из потока состава. Пул подменён объектом, падающим на любое
  обращение: герметичность — проверка, а не декларация, поэтому `make test-contract` работает без
  стенда и без служебных контейнеров в CI.
- `unit/domain/test_statuses.py`: задача 001.28 — таблицы §4.6 как код: список статусов теста
  сверяется с самим типом `NodeStatus` (иначе девятый статус не попал бы ни в одну таблицу, а
  вентиль дал бы KeyError и 500), матрица переходов целиком **равенством** плюс 16 отдельных пар,
  включая два чтения, которые таблица прямо не называет, приоритет ручного статуса, вентиль
  потоков равенством по всем статусам, `filters_composition` ровно там, где ответ отфильтрован.
- `unit/domain/test_composition.py`: задача 001.28 — контрольная сумма (каноническая запись
  выписана независимо от реализации; перестановка ключей не меняет сумму, изменение значения —
  меняет), каркас конфигурации без клиентов, устойчивый тег `u{user_id}`, credentials на пару
  «пользователь × нода» (§11.3: разные у разных пользователей и у одного пользователя на разных
  нодах, не равные идентификатору), credentials нет только у `removed`, и самое свежее изменение
  состава доступ не снимает — без этого свойства данных правило «курсор идёт по выборке до
  фильтра» не отличалось бы от «курсор равен константе».

## Conventions
- Секреты только файлами (`*_FILE` в окружении: `PG_PASSWORD_FILE`, `MIGRATE_PASSWORD_FILE`,
  `APP_ENCRYPTION_KEY_FILE`, `CA_KEY_FILE`, `SMTP_PASSWORD_FILE`), адреса служб — `PG_DSN`
  (роль `app_rw`), `MIGRATE_DSN` (роль `app_migrate`), `REDIS_URL` (§10.3).
- Миграции — только через `python -m app.cli migrate` (yoyo API, блокировка; `--break-lock`
  после клиента, умершего посреди миграции; `--unmark ID` после клиента, умершего между откатом
  и снятием отметки — самопроверка `migrate` укажет на такую отметку кодом 65) или CLI yoyo с
  `yoyo.ini`; SQL к базе без ORM (asyncpg в приложении, psycopg 3 у yoyo).
- Обновление `requirements*.lock` — вместе, из одного pyproject.
