# Контекст сборки control-plane/Dockerfile: app/, migrations/, yoyo.ini, requirements.lock,
# docker-entrypoint.sh — всё остальное в образ не попадает.
*
!app/
!migrations/
!yoyo.ini
!requirements.lock
!docker-entrypoint.sh
**/__pycache__/
**/.AGENTS.md
