# Directory: control-plane/app/

## Purpose
Приложение Control Plane (C-01 `api`, C-02 `worker`, C-03 `scheduler`) — один пакет Python,
запускаемый в трёх ролях. Границы модулей — по функциональным компонентам
`docs/architectures/functional-architecture.md`.

## Files

### [__init__.py]
- `__version__` — версия приложения (`info.version` в OpenAPI); поднимается при выпуске.

### [main.py]
- `create_app() -> FastAPI` (001.10): роутеры `/api/v1`, `/agent/v1`, `/s`, обработчики единого
  формата ошибок, `/healthz` (живость; готовность базы/Redis — 001.68), `/metrics` (текст
  Prometheus, до 001.68 один показатель), OpenAPI по `/openapi.json` без `/docs`. Сборка и
  lifespan к базе и Redis не обращаются; lifespan только закрывает пул и клиент. Точка входа
  uvicorn — `app.main:create_app --factory`.

### [config.py]
- `Settings` (pydantic-settings, alias = имя переменной): `PG_DSN`, `PG_PASSWORD_FILE`,
  `REDIS_URL`, `APP_ROLE`, `APP_ENV`, `LOG_LEVEL`, `SUBSCRIPTION_DOMAINS` (через запятую,
  `NoDecode`), `APP_ENCRYPTION_KEY_FILE`. `Settings.load()` читает окружение и проверяет файлы
  секретов (`SecretError` при отсутствии или пустоте — ошибка старта). `read_secret()`,
  `dsn_with_password()` — общие для приложения и `cli.py`; `pg_dsn_with_password` — DSN `app_rw`;
  `Settings.read()` — окружение без проверки файлов секретов (несекретные значения на запрос,
  например домены подписки); роль `api` не стартует с пустым `SUBSCRIPTION_DOMAINS`
  (валидатор модели).

### [errors.py]
- `ApiError(code, message, status=, details=, headers=)` и `error_response()` — формат
  `{"error": {"code", "message", "details"}}` (interfaces.md §5.1); `install_error_handlers()`
  переводит в него `HTTPException` (404 `not_found`, 405 `method_not_allowed`, …),
  `RequestValidationError` (422 `validation_error`, `details.errors[loc, msg, type]`) и любые
  исключения (500 `internal_error` без подробностей, трассировка — в журнал);
  `RateLimitUnavailable`, `DatabaseUnavailable`, `asyncpg.PostgresConnectionError`,
  `redis.exceptions.ConnectionError`/`TimeoutError` → 503 с `Retry-After` (fail-closed §9.1,
  001.14/001.15; прочие ошибки Redis — 500 с трассировкой). `not_implemented(operation)` — 501 заглушек STUB-задач.

### [metrics.py]
- `render()` — текст Prometheus для `/metrics`: `control_plane_up`, `control_plane_db_up`,
  `control_plane_jobs{queue,status}` (все пары, нули включены; ряд `status="dead"` — DLQ,
  источник алерта), `control_plane_jobs_wait_seconds{queue}` (наибольшее ожидание: выбранные
  за 5 минут и ещё не выбранные готовые) и `control_plane_jobs_wait_budget_seconds` (5 с для
  `critical` — крыша, половина Н-13); база недоступна → `control_plane_db_up 0`, ответ не
  падает. Библиотека клиента и гистограммы — 001.68.

### [i18n.py]
- `negotiate(user, accept_language) -> str` — заглушка 001.10 (`en`); реализация — 001.65.

### [redis.py]
- `get_redis()` — один клиент `redis.asyncio` на процесс, создаётся лениво из `Settings`
  (таймауты 1 с / 2 с — fail-closed отвечает быстро); `close_redis()`. Клиент привязан к циклу
  событий: объект чужого цикла отбрасывается (тесты). Имя модуля совпадает с пакетом `redis`:
  внутри — только абсолютные импорты.

### [db/pool.py]
- `get_pool()` — один пул asyncpg на процесс под `app_rw` (лениво, `min_size=1`, `max_size=10`,
  таймаут подключения 5 с; отказ или таймаут → `DatabaseUnavailable`); `db_pool()` — зависимость
  FastAPI для маршрутов (`Depends(get_pool)` напрямую сделал бы `settings` телом запроса);
  `close_pool()`;
  `transaction(pool)` — подключение в транзакции (commit / rollback). Пул привязан к циклу
  событий: пул закрытого цикла обрывается и создаётся заново (тесты).

### [cli.py]
- `python -m app.cli migrate [--rollback | --rollback-all | --unmark ID] [--break-lock]` —
  миграции yoyo из `../migrations` под ролью `app_migrate` (`MIGRATE_DSN` +
  `MIGRATE_PASSWORD_FILE` или полный URL), с блокировкой yoyo; вызывается точкой входа образа при
  старте роли `api` (§10.2). Самопроверка учёта перед применением и откатом (WI-3): у каждой
  применённой миграции должен существовать ключевой объект (`key_object()` — первый `CREATE
  TABLE`/`TYPE` файла со схемой из его `search_path`; миграции только с данными не охватываются);
  отметка без объектов (клиент умер между откатом и снятием отметки) → код 65
  (`EX_MARK_WITHOUT_OBJECTS`) с подсказкой, ремонт — `--unmark ID` (без самопроверки), затем
  `migrate`.
  `migrate_dsn()` нормализует схему в `postgresql+psycopg` через `config.dsn_with_password()`.
  `python -m app.cli admin create` — заглушка до 001.47 (код 69).

## Subpackages (каркас задачи 001.01; содержимое добавляют задачи плана)

| Пакет | Назначение | Задачи |
| :--- | :--- | :--- |
| `api/` | `/api/v1` — кабинет и панель; `router.py` с подроутерами `auth`, `me`, `admin/` (пакет: `plans`, `groups`, `codes`, `nodes`, `dashboard`; карта — `api/.AGENTS.md`) | 001.10, 001.13…001.20, 001.24, 001.46…001.50 |
| `agent_api/` | `/agent/v1` — Node API; `router.py` подключает `enroll.py`, `state.py`, `heartbeat.py`, `commands.py`. `deps.py` (001.28) — `supported_agent_version` (`X-Agent-Version`: три числа ASCII с необязательным суффиксом предвыпуска, `MIN_AGENT_VERSION` (0,1,0), длина ≤ `AGENT_VERSION_MAX_CHARS` 64; отсутствие, неразбор и версия ниже — 426 с `Retry-After`), `current_node` (`X-Client-Fingerprint` — hex SHA-1 или SHA-256, `X-Node-Identity` — base64url 32…128 символов; иначе 401; поиск в `node_identities` — 001.25) и `served_node` (§4.6: ноде в `pending` раздел не выдаёт ничего — один вентиль на все операции), фабрики служб через `Depends(db_pool)` — `Streams`, `Commands`, `Statuses`, `Served`. `enroll.py` (001.24): `POST /enroll` — `EnrollIn(bootstrap_token 32…128, csr_pem — PEM-блок CERTIFICATE REQUEST, agent_version, xray_version)`, `extra="forbid"` → `EnrollOut(client_cert_pem, ca_pem, identity_token, node_id)`; единственный маршрут раздела без всех трёх заголовков — отдельный `server` nginx; лимит частоты по адресу и хешу токена — 001.72. `state.py` (001.28): `GET /state` (курсоры обязательны в обеих формах, `full` — снапшот; 200 `StateResponse` сериализуется моделью напрямую с `by_alias`, без повторной проверки `response_model`, 204 «изменений нет», 403 `pending`, 409 чужое поколение, 429 объявлен под 001.72; `Cache-Control: no-store` — в ответе credentials) и `POST /ack` → 204. `heartbeat.py`: `POST /heartbeat` (версии, время ноды со смещением, `applied_*` — подтверждает курсоры наравне с `/ack`) и `POST /metrics` → 204. `commands.py`: `POST /commands/{id}/result` → 204. Отчёты о трафике и грант квоты — 001.33 | 001.10, 001.24, 001.28, 001.33 |
| `subscription/` | `/s/{token}`, генераторы форматов, страница подписки; `router.py` (заглушка) | 001.10, 001.41…001.45 |
| `domain/` | `users.py` — `normalize_email` (§16.8: нижний регистр всего адреса), `hash_token`, `CaptchaVerifier`/`NoCaptchaProvider` (ОВ-A3, fail-closed), `CaptchaAnswer` (ответ одного запроса: провайдер спрашивается не более одного раза — ответы одноразовые; `available`, `verified()`), `UserService(pool, captcha)` (`captcha_available`, `captcha_answer(token, ip)`): `register(…, captcha=CaptchaAnswer)` (режим `settings.registration_mode`, одноразовые домены, CAPTCHA по настройке тем же ответом, `INSERT … ON CONFLICT (email)` → 409, `email_tokens` verify со сроком `settings.email_token_ttl_minutes` (15 мин по умолчанию, ОВ-25; `email_token_ttl()` отвергает не целое/< 1), задача `send_email` с токеном, `auth_events`), `verify_email`, `authenticate` (argon2id; хеш-пустышка для неизвестного адреса; нужны подтверждённый адрес, `active`, не удалён), `request_reset` (reset с тем же сроком из настройки; один набор запросов для любого адреса — `INSERT … SELECT` токена и задача `reset`/`reset_unknown`), `confirm_reset`/`confirm_reset_user`; `profile.py` — `Profile` (язык, пояс IANA, согласие), `ProfileService(pool)`: `get`/`update`/`delete_account` — заглушки 001.15 (`STUB_PROFILE` под идентификатором вызывающего; логика 001.16, удаление 001.17); `plans.py` — схемы `PlanIn`/`PlanPatch`/`Plan` (§4.10, `inbound_profile`, валюта ISO 4217; `PATCH`: непереданное не меняется, `null` сбрасывает необязательное, `null` для NOT NULL → 422) и `PlanService` — заглушка 001.18 (логика 001.19); `groups.py` — группы доступа, тарифицируемые группы, `MultiplierIn` (0…10 с шагом 0.1, UC-09 A2), `GroupService` — заглушка; `codes.py` — формат `VPN-XXXX-XXXY`, `checksum_ok`/`checksum_char`/`normalize_code`/`generate_code` (реализованы: алфавит 32 без I/O/0/1, нечётные веса mod 32; выдача — `secure_source()` = `SystemRandom`), `csv_rows` (RFC 4180 + защита от формул), `CodeSpec`/`Code`/`Redemption`, `CodeService` — заглушка, `redeem` отклоняет неверную сумму без базы (логика 001.20); `nodes.py` — схемы §4.2.3: `NodeIn` (код, имя, страна ISO alpha-2, город, провайдер, IPv4/IPv6, fqdn, `billing_group_id` NOT NULL R-18, группы доступа, `bandwidth_mbps` > 0 §5.9, `max_conn_per_ip` > 0 Н-29, `legal_profile`, экономика §4.15), `NodePatch` (те же правила `null`, что у тарифов), `Node` (статус `node_status` §4.6, версии агента и Xray, heartbeat), `BootstrapToken` (`BOOTSTRAP_TOKEN_TTL` 60 мин Н-24, токен 256 бит `generate_bootstrap_token`), `ManualStatusIn` (`maintenance|disabled|suspended|null` — снятие), `Identity`/`Enrollment` (отпечаток SHA-256 DER), `Cursors`/`Heartbeat`/`NodeState`; `NodeService(pool, ca)` — заглушка 001.24: `list/create/get/update/decommission`, `issue_bootstrap_token` (настоящий случайный токен и срок, без записи), `enroll` (подпись CSR через `InternalCA`, identity фиксированная), `approve` (`provisioning`), `set_manual_status`, `revoke_identity` (`revoked_at` в состоянии), `state`; логика — 001.25, статусы по heartbeat — 001.30; `statuses.py` (001.28) — таблицы §4.6 как данные: `TRANSITIONS` + `StatusService.can_transition` (матрица переходов; инициатор перехода не измеряется — 001.30), `STREAM_GATE` из `StreamGate(refused, config, composition, commands)` со свойством `filters_composition` (матрица влияния плюс столбец конфигурации §5.2), `MANUAL`/`AUTOMATIC`, `INT4_MAX`/`INT8_MAX` (ширина колонок §4.2.3), схемы `HeartbeatIn` и `NodeMetrics` (колонки `node_metrics` без `node_id`); `on_heartbeat(node_id, at, beat)` — отметка временем сервера, тело целиком рядом: время ноды для сверки перекоса §5.9 и версии агента и Xray (единственное место, где нода сообщает их после enrollment); `on_missed_heartbeat`, `on_metrics` — заглушки, автоматика Н-15 и пороги §4.7 — 001.30. `composition.py` (001.28) — `StateResponse{config?: StateConfig(version, checksum, json — псевдоним поля `document`), users: Users(seq, full, rows[UserRow]), generation, resync_required, commands[]}`, `Credentials` (только у строк `active`: снять доступ нода умеет по тегу, и отключённой ноде живые ключи не отправляются), `AckIn`, `REVOKING_STATES`, `xray_email(user_id)` = `u{id}`, `config_checksum` (SHA-256 канонического JSON — правило и ограничения документа в `contracts/agent_v1/README.md`), `node_not_approved`/`generation_mismatch` (403 и 409 с `Retry-After`); `CompositionService.state_for` применяет вентиль §4.6 к обеим формам запроса (снапшот его не обходит), сверяет поколение, отклоняет курсор впереди серверного (`cursor_ahead`, 409 — молчаливый 204 отключил бы ноде доставку отзывов), считает отставание только по открытым вентилем потокам (иначе `provisioning` не дошёл бы до 204), читает курсор **до** строк и продвигает его по выборке до фильтра по статусу, помечает `users.full` только у неурезанного состава и снимает `resync_required` в таком ответе (иначе нода снапшотится по кругу); `ack(node, …)` отклоняет подтверждение от неподтверждённой ноды; `publish_config`, `publish_user`, `remove_user` — заглушки, логика 001.29, снапшот и long-poll — 001.75. `commands.py` (001.28) — `Command` (сроки — `AwareDatetime`), `CommandResultIn` (от ноды только `applied|failed|expired`; `issued`/`delivered` ставит Control Plane), `CommandService.issue`, `pending(node)`, `result(node, command_id, …)` — ноду принимают целиком, потому что канал команд вентилирует §4.6; заглушки, очередь команд 001.76; далее подписки, коэффициенты, состав | 001.13, 001.14, 001.18…001.31 |
| `accounting/` | `stats.py` — схемы §4.12 (`TrafficStats`, `NodeTraffic`, `CountryTraffic`, `PeriodRef`; байты целые, коэффициент — десятичная строка) и `user_traffic(conn, user_id, period_id)` — заглушка `STUB_STATS` (001.15; агрегация `traffic_hourly` — 001.16); далее приём отчётов, лимиты, гранты, сверки, агрегация | 001.15, 001.16, 001.33…001.40, 001.77 |
| `jobs/` | очередь `jobs` в PostgreSQL (`queue.py`: `enqueue` → `Enqueued(id, created)` — активный дубль возвращает существующую задачу; `claim(types, lease)` берёт готовые pending и брошенные running с истёкшей арендой `LEASE` 5 мин, ставит `claimed_at`; `complete`/`fail`/`bury`(dead)/`retry`(pending + run_at) — с `owner`: только строка, которую держит этот исполнитель, иначе `LookupError`; `depth`, `wait_seconds(window)` — выбранные за окно и не выбранные готовые; `NonRetryableError`), исполнители (`worker.py`: LISTEN + опрос, `run_once`, `decide` → retry/dead/failed, `backoff` 1-2-4 с с джиттером до `BACKOFF_CAP`, dead — ERROR в журнале; `CRITICAL_WAIT_BUDGET_SECONDS` 5; переживает ошибки базы и обрыв LISTEN — `recover`, `RECOVERABLE`), планировщик (`scheduler.py`: advisory-блокировка с параметром `lock_key`, `Periodic`, `SCHEDULE` = `ensure_partitions` раз в час; `tick(..., placed)` помнит поставленные слоты — ключ свободен после `done`), реестр обработчиков `handlers/` (`noop`, `ensure_partitions` → `partitions.py`: `ensure_partitions(7)` + `drop_expired_partitions()`, `composition.publish_user` → `composition.py`: заглушка 001.28, запись состава — 001.29) | 001.11, 001.14, 001.28, 001.37, 001.74 |
| `security/` | `passwords.py` (argon2id §7.2), `crypto.py` (`FieldCipher` AES-256-GCM, контейнер версия‖nonce‖шифртекст), `sessions.py` (`SessionStore`: `create` → `Session` (с `csrf`), `get` (скользящий TTL), `revoke`, `revoke_all(subject, except_sid)`; хеш `sess:{sid}`, набор `user_sessions:{subject}` с TTL сессии и чисткой мёртвых членов при создании; cookie `sid`, `USER_SESSION_TTL` 30 суток), `csrf.py` (double-submit: cookie `csrf` + заголовок `X-CSRF-Token`, `require_csrf` → 403 `csrf_failed`), `ratelimit.py` (ключи §5.12, пороги `Limit` — `LOGIN_IP`, `LOGIN_ACCOUNT`, `REGISTER_IP`, `REGISTER_DOMAIN`, `RESET_EMAIL`, `TOKEN_IP`; `RateLimiter.ensure_available` fail-closed, `check` — скользящее окно ZSET Lua-скриптом (`register_script`), `peek` без учёта попытки, `retry_after`, `reset`), `deps.py` (`redis_required` действует; `current_user` → `CurrentUser(id, session)` по cookie `sid` — только сессии вида `user` с UUID-субъектом, иначе 401 `unauthenticated` (001.15); `current_admin` → `CurrentAdmin(id, session)` — сессии вида `admin`, минимальный вид 001.18 до TOTP 001.47; `require_permission` — заглушка до 001.46, операции панели объявляют `x-permission`), `ca.py` (001.24: `pem_der(pem, label)` — DER одного PEM-блока с проверкой метки и base64; `InternalCA.fingerprint` — SHA-256 DER hex (реализовано, как в `node_identities.cert_fingerprint`); `sign_csr(csr_pem, days=90)` — заглушка: требует PEM `CERTIFICATE REQUEST`, возвращает `STUB_CLIENT_CERT_PEM`; `ca_pem` — `STUB_CA_PEM`; ключ из `CA_KEY_FILE` и подпись через `cryptography` — 001.25); далее TOTP, RBAC, аудит | 001.12, 001.14, 001.24, 001.84, 001.46…001.48 |
| `notifications/` | события, почта, webhook | 001.51, 001.52, 001.78, 001.79 |

## Conventions
- Python 3.14; SQL без ORM (asyncpg); настройки — `pydantic-settings`; проверки — `ruff`, `mypy --strict`.
- Ошибки API — только `ApiError` / `not_implemented()`; тело всегда `{"error": …}` (§5.1);
  `RateLimitUnavailable` → 503 с `Retry-After` (fail-closed §9.1) — роутеры операций с лимитом
  частоты подключают `Depends(redis_required)`.
- Новые роутеры регистрируются в `api/router.py`, `agent_api/router.py`, `subscription/router.py`;
  `main.py` подключает три корневых роутера и не знает о подроутерах.
- Пул и Redis — через `get_pool()` / `get_redis()`, без собственных подключений в модулях
  (исключения: подключение LISTEN исполнителя и сессия-держатель advisory-блокировки
  планировщика — намеренно вне пула).
- Постановка задач — только `app.jobs.queue.enqueue` внутри транзакции доменной операции;
  обработчики регистрируются в `jobs/handlers/__init__.py::HANDLERS`, периодические задачи — в
  `jobs/scheduler.py::SCHEDULE`. Исполнитель выбирает только задачи типов из `HANDLERS`:
  задачи неизвестных типов ждут исполнителя, который их знает (поэтапный выпуск).
- Долгоживущие циклы (исполнитель, планировщик) не умирают от ошибок базы: `RECOVERABLE` →
  журнал, `pool.expire_connections()`, пауза, продолжение; ошибки программы — наружу.
- Состояние в процессе не хранится (§4.20 постановки): сессии и счётчики — в Redis.
- Мутации под сессией подключают `Depends(require_csrf)` (§7.3); cookie `csrf` выставляется при
  входе рядом с `sid`, клиент возвращает её значение в `X-CSRF-Token`.
- Ответы, чувствительные к существованию учётной записи (восстановление, вход), идут одним путём
  кода и с одинаковой стоимостью (задача-пустышка, хеш-пустышка) — UC-15 A1.
