# Отчёт о проверке — задача 001.74 «Очередь задач: повторы, экспоненциальная задержка, DLQ с алертом, бюджет ожидания»

Дата: 2026-09-09 (раунд 2 после ревью — см. «Раунд 2»; разделы ниже описывают текущее
состояние кода). Стенд: VM (`ssh vm`), Compose `control-plane` — все роли `Up`,
миграция `110_jobs_retry` применена (9 миграций). Тесты — с рабочей машины против живых базы и
Redis стенда рядом с работающими исполнителями и планировщиком (свои типы `test-*`).

## Регрессия

- `make check` (nvm use 24, `PG_DSN`/`MIGRATE_DSN`/`REDIS_URL` → стенд) → код 0: ruff, ruff
  format, mypy strict (81 файл), pytest **222 passed** (прежние 214 + 001.74: 8), go, web.
  Первый полный прогон уронил страж каталога 001.09 (`test_catalog_matches_data_model`: колонки
  `jobs` расходятся с §4.2) — ожидаемо для новой миграции, спецификация дополнена.
- `cd control-plane && pytest tests/unit/jobs tests/e2e/test_jobs.py` → 22 passed.
- tdd-strict: `tests/unit/jobs/test_worker.py` (3) и `test_queue.py::test_enqueue_result_carries_id_and_creation_flag`
  были красными (`AttributeError`) до реализации.

## Стенд

`vm-sync`, `up -d --build`, роли `Up`; `/metrics` изнутри сети стенда (снаружи `/metrics` закрыт
nginx — 001.10):

```text
control_plane_jobs{queue="critical",status="dead"} 0      # DLQ — ряд 001.11, источник алерта
control_plane_jobs_wait_seconds{queue="critical"} 0.000
control_plane_jobs_wait_seconds{queue="background"} 0.007
control_plane_jobs_wait_budget_seconds{queue="critical"} 5
```

## Сквозные тесты (`control-plane/tests/e2e/test_jobs.py`, +3 и адаптация 001.11)

1. **TC-E2E-01** `test_dead_after_max_attempts_with_growing_backoff`: `max_attempts = 3`, база
   задержки 0,2 с — после первой и второй ошибки задача `pending` с `run_at` в будущем (до него не
   выбирается), задержки по часам базы `[0,2; 0,4) ≤ … < [0,4; 0,8)`; третья ошибка → `dead`,
   `attempts = 3`, текст ошибки, `finished_at`, запись ERROR исполнителя с номером задачи и словом
   `dead`, ряд `control_plane_jobs{queue="background",status="dead"} ≥ 1`; `dead` не выбирается;
   ключ свободен.
2. **TC-E2E-02** `test_critical_job_wait_is_within_half_of_n13_budget`: исполнитель критичной
   очереди в процессе теста; постановка → выполнение по NOTIFY, `claimed_at − created_at ≤ 1 с`
   (≤ бюджета 5 с); ряды `control_plane_jobs_wait_seconds{queue="critical"}` и бюджета `= 5`
   присутствуют (значение ряда — по всему стенду, чужие строки могут ждать дольше).
2а. `test_abandoned_running_job_is_reclaimed_after_lease`: `running` с `locked_at` старше аренды
   (1 с в тесте) отбирается другим исполнителем с `attempts = 2`, свежая `running` — нет; прежний
   владелец («зомби») получает `LookupError` на `complete`/`fail`/`bury`/`retry`, строка не
   меняется; боевая аренда в пределах 1…15 минут.
2б. `test_wait_seconds_measures_backlog_and_claimed_delay`: готовая, но не выбранная задача с
   `run_at = now − 30 с` даёт ожидание ≈ 30 с; после выборки — то же (`claimed_at − run_at`);
   строка в ожидании повтора не учитывается (0, не отрицательное); после удаления — фон.
3. `test_failures_unknown_type_and_run_at` (001.11, переписан): `boom` → `pending` с
   `last_error` и задержкой `[1; 2) с` от `claimed_at`; `test-fatal` (`NonRetryableError`) →
   `failed` сразу; задача в ожидании повтора держит ключ, после `failed` ключ свободен.
4. `test_job_cycle_and_idempotency` (001.11, переписан): дубль активного ключа — `Enqueued` с
   тем же id и `created = False` (и для `running`), транзакция жива; `claimed_at ≤ finished_at`.
5. `test_scheduler_leadership_lock`: `tick` считает только созданные задачи.
6. `test_metrics_report_queue_depth`: ряды ожидания по обеим очередям рядом с рядами по статусам.

## Модульные тесты

- **TC-UNIT-01** `unit/jobs/test_worker.py::test_backoff_doubles_with_bounded_jitter`: попытки
  1, 2, 3 → 1, 2, 4 с при нулевом джиттере и 1,25 / 2,5 / 5 при максимальном; случайный джиттер
  в `[0; 25 %)`; `test_backoff_is_capped_and_scalable` — потолок 300 с, база масштабируется,
  попытка 0 отклоняется; `test_decision_retry_dead_or_fail` — retry/dead/failed.
- `unit/jobs/test_queue.py::test_enqueue_result_carries_id_and_creation_flag`: `Enqueued` — id и
  признак создания (именованный кортеж), `DuplicateJobError` отсутствует, `CLAIM_SQL` содержит
  аренду брошенных `running`.

## Посадки стражей (до ревью, по одной; файлы восстановлены, `cmp` совпадает)

| Посадка | Результат |
| :--- | :--- |
| 1 попытки не исчерпываются (нет `dead`) | e2e: статус `pending` ≠ `dead` |
| 2 задержка не растёт | e2e: `[0.236, 0.249]` — вторая не ≥ 0,4 |
| 3 повтор без задержки (`run_at = now`) | e2e: `5 == 3` — повтор выбран в той же итерации |
| 4 `claimed_at` не записывается | e2e: `isinstance(None, datetime)` |
| 5 дубль отчитывается как созданный | e2e: `(51, True) == (51, False)` |
| 6 `NonRetryableError` повторяется | e2e: `pending` ≠ `failed` |
| 7 `jobs_dead_total` всегда 0 | e2e: `0 >= 1` |
| 8 `dead` без записи ERROR | e2e: «событие для алерта — запись ERROR в журнале» |

После посадок: 20 passed.

## Раунд 2 — правки по ревью и их проверка

Ревью раунда 1 (`sarcasmotron-001-74`, REJECTED): C-01 задача, чей исполнитель умер в
`running`, не восстанавливалась никогда (ни повтора, ни `dead`, ключ занят навсегда; доказано
зондом на стенде), а комментарий над `CLAIM_SQL` обещал обратное; T-01 метрика ожидания без
стража на величину (посадка «всегда 0» — зелёная); L-01 метрика слепа к стоящей очереди
(только выбранные строки); L-02 строки в ожидании повтора давали отрицательную пару времён;
S-01 `jobs_dead_total` дублирует `control_plane_jobs{status="dead"}` и нарушает соглашение о
суффиксе; S-02 `Enqueued(int)` ломает `copy`/`pickle`; S-03 неограниченная рекурсия в
`enqueue`; D-01 правило нумерации миграций отстало; D-02 бюджет 5 с против строки 1 с в §5.4;
N-01…N-04.

- **C-01:** аренда выборки — `claim(lease=LEASE)` (5 минут) забирает `running` с `locked_at`
  старше аренды как новую попытку (`CLAIM_SQL`, комментарий переписан);
  `test_abandoned_running_job_is_reclaimed_after_lease`. Посадка ревьюера (ветка `running`
  убрана) → «брошенная задача выбрана снова: None is not None».
- **T-01 / L-01 / L-02:** `wait_seconds` — объединение «выбранные за окно с
  `claimed_at ≥ run_at`» и «не выбранные готовые (`now() − run_at`)»;
  `test_wait_seconds_measures_backlog_and_claimed_delay` проверяет величину (≈ 30 с). Посадки:
  константный 0 → `28 <= 0.0`; без ветки backlog → то же; без фильтра `claimed_at ≥ run_at` —
  зелёная по построению (агрегат — максимум, отрицательное значение его не меняет; фильтр
  оставлен ради корректности документации и будущих перцентилей).
- **S-01:** ряд `jobs_dead_total` снят; DLQ — `control_plane_jobs{status="dead"}`, правило
  алерта `sum(...) > 0` (объявлено). **S-02:** `Enqueued` — `NamedTuple`. **S-03:** две попытки
  вставки, затем `RuntimeError`. **D-01:** правило нумерации в карте переписано (по порядку
  применения, номер задачи — в заголовке). **D-02:** бюджет назван крышей (HELP метрики,
  «Уточнения»). **N-01:** индекс `jobs_claimed_at_idx` (миграция 110, откат `IF EXISTS`),
  запросов `/metrics` — два. **N-04:** тест бюджета проверяет ожидание своей задачи, а не
  максимум по стенду.

После правок: `make check` → 0, **222 passed**; миграция 110 переприменена на стенде с индексом
(откат → применение), стенд пересобран, все роли `Up`, `dead` нет.

## Раунд 3 — правки по ревью раунда 2

Ревью раунда 2 (`sarcasmotron-001-74`, REJECTED): C-01 (новое) аренда без фехтования владельца
— медленный, а не мёртвый исполнитель закрывал чужую перехваченную задачу как `done` или
возвращал её в очередь (доказано зондом на стенде); D-01 пять висячих упоминаний
`jobs_dead_total` и самопротиворечие в «Уточнениях»; D-02 индекс и новое определение ожидания
не отражены в `data-model.md`; N-01 ветка backlog считает и задачи неизвестных типов; N-02
хрупкость теста величины ожидания.

- **C-01:** `complete`/`fail`/`bury`/`retry` принимают `owner` и меняют строку только при
  `locked_by = owner`; исполнитель передаёт свой идентификатор; тест аренды дополнен: зомби
  получает `LookupError` на всех четырёх переходах, строка остаётся у нового владельца.
  Посадка (фехтование снято на обоих запросах) → «DID NOT RAISE LookupError». `interfaces.md`
  §5.4 уточнён: один исполнитель на задачу в каждый момент, брошенная выбирается снова,
  завершение фехтуется владельцем, выполнение at-least-once.
- **D-01:** `jobs_dead_total` вычищен из кода, задачи и отчёта. **D-02:** `data-model.md`
  §4.2.6 — индекс `claimed_at` и определение ожидания для выбранных и невыбранных. **N-01:**
  оговорка для правила 001.68 записана в «Уточнения». **N-02:** оставлено (страж величины
  нуждается в известном `N`; чужая критичная строка на стенде — след другого теста и убирается
  им).

После правок: `make check` → 0, 222 passed.

### Вердикт раунда 3

`sarcasmotron-001-74`: **APPROVED**. Посадки ревьюера: фехтование снято только у `retry` →
«DID NOT RAISE LookupError»; `process_one` завершает от имени другого владельца → 6 тестов
красных (проводка владельца от выборки до завершения). Зонд раунда 2 на живом стенде: зомби
получает `LookupError` на всех переходах, попытки не сожжены. Вкусовщина (рваный перенос в
docstring `worker.py`) исправлена сразу.

## Критерии приёмки

- [x] Повторы, `dead` и алерт реализованы — тесты 1, 3; посадки 1–3, 6–8
- [x] Ожидание задачи критичного пути ≤ половины бюджета Н-13 (метрика) — тесты 2 и 2б, стенд

## Отклонения от описания задачи

- `NOTIFY` без обёртки `transaction()` (доставка при коммите — свойство PostgreSQL, тест 001.11);
  `enqueue` возвращает `Enqueued(id, created)`, `DuplicateJobError` удалён; аренда выборки
  `LEASE` 5 мин; `NonRetryableError` → `failed`; событие алерта — запись ERROR + ряд
  `control_plane_jobs{status="dead"}` вместо `jobs_dead_total`; метрика ожидания учитывает
  не выбранные готовые задачи; метрики в `app/metrics.py`; миграция `110_jobs_retry`
  (`claimed_at`, `finished_at`, индекс); адаптация тестов 001.11. Всё объявлено в «Уточнениях при реализации»; карты
  `app/.AGENTS.md`, `control-plane/.AGENTS.md`; `data-model.md` §4.2.6 дополнен.
