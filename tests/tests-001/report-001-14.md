# Отчёт о проверке — задача 001.14 «API аутентификации: регистрация, подтверждение адреса, восстановление пароля»

Дата: 2026-09-09 (после трёх ревью: порог по учётной записи без запирания владельца, гонка
регистрации, симметричный путь восстановления и строгий страж времени, TTL множества сессий,
`Retry-After`, вход заблокированной записи, CAPTCHA на порогах регистрации и входа по адресу
источника, один вызов провайдера CAPTCHA на запрос — см. «Раунд 2», «Раунд 3», «Раунд 4»;
разделы ниже описывают текущее состояние кода).
Стенд: VM (`ssh vm`), Compose
`control-plane` — все роли `Up`
(`api` healthy, `worker-critical`, `worker-background`, `scheduler`), миграции 8 (добавлена
`100_settings_auth`). Тесты — с рабочей машины через `ASGITransport` против живых базы и Redis
стенда, рядом с работающими исполнителями и планировщиком (без остановки контейнеров).

## Регрессия

- `make check` (nvm use 24, `PG_DSN`/`MIGRATE_DSN`/`REDIS_URL` → стенд, живые контейнеры) → код 0:
  ruff, ruff format, mypy strict (75 файлов), pytest **192 passed** (прежние 158 + 001.14: 34),
  go, web.
- `cd control-plane && pytest tests/e2e/test_auth.py tests/unit/security tests/unit/domain` →
  69 passed (после посадок, файлы восстановлены).
- Первый полный прогон: 1 failed — `test_login_rejects_when_authenticate_returns_none` получал
  429: клиент `app_client` с общим адресом 127.0.0.1 копил счётчик входа между прогонами (порог
  5/60 с). Тест переведён на клиент со своим адресом источника (`auth_stand`), шесть прогонов
  подряд зелёные.

## Стенд (через nginx, `curl -k https://127.0.0.1`)

```text
POST /auth/register {email, password, aup_version}   → 201; повтор того же адреса → 409 email_taken
POST /auth/login до подтверждения                     → 401 invalid_credentials
POST /auth/verify {token из очереди send_email}       → 200 confirmed; повтор → 410 token_expired
POST /auth/login                                      → 200 ok; set-cookie: sid=…; HttpOnly; Max-Age=2592000; Path=/; SameSite=lax; Secure
                                                              set-cookie: csrf=…; Max-Age=2592000; Path=/; SameSite=lax; Secure
POST /auth/logout с cookie без X-CSRF-Token           → 403;  с заголовком → 204
POST /auth/reset-request ×3 (один адрес)              → 202 202 202; четвёртый → 429 rate_limited
POST /auth/reset-request неизвестный адрес            → 202 {"status":"accepted"}
auth_events пользователя: register|success, login|denied, login|success
```

Следы прогона убраны (пользователи `@test-auth.local`, их события, задачи `send_email`, ключи
`rl:*`). Планировщик после исправления памяти слотов: одна задача `ensure_partitions` на слот
(до исправления — новая задача каждую секунду после `done`: 48 строк за минуты).

## Сквозные тесты (`control-plane/tests/e2e/test_auth.py`, 36 тестов; `_auth.py` — стенд теста)

1. `test_seven_routes_in_openapi_with_schemas`, `test_validation_errors_use_unified_format` ×14 —
   из 001.13, без изменений.
2. **TC-E2E-01** `test_uc02_full_registration_cycle`: регистрация → 201 без cookie, запись
   `users` с нормализованным адресом, `aup_version`/`aup_accepted_at`, `email_verified_at` пуст;
   письмо `verify` в очереди `send_email`; вход до подтверждения → 401; `verify` → 200; вход →
   200 с cookie `sid` и `csrf`; `auth_events`: `register|success`, `login|denied`, `login|success`.
3. **TC-E2E-02** `test_uc02_a1_duplicate_email_is_rejected`: тот же адрес в другом регистре →
   409 `email_taken`, второе письмо не ставится.
4. **TC-E2E-03** `test_uc02_a5_login_rate_limit_by_ip_does_not_block_account`: шесть неверных
   паролей с одного адреса источника → `[401 ×5, 429]` с `Retry-After` 1…60; та же учётная
   запись с другого адреса входит верным паролем → 200 (AC-36).
5. `test_uc02_a5_account_threshold_never_locks_owner_without_provider`: десять отказов по
   учётной записи с пяти адресов (ключ Redis содержит 10 отметок), затем владелец с верным
   паролем → 200 и счётчик обнулён; следующий отказ → 401 и счётчик = 1; три успешных входа
   подряд счётчик не тратят; другая учётная запись входит.
   `test_uc02_a5_account_threshold_requires_captcha_with_provider` (провайдер подменён через
   `dependency_overrides`): после десяти отказов верный пароль без ответа → 429
   `captcha_required` с `details.captcha_required` и без cookie, с неверным ответом → 429, с
   верным → 200 и счётчик обнулён; верный ответ с неверным паролем → 401 и отказ засчитан.
   `test_uc02_a5_registration_threshold_offers_captcha_with_provider`: шестая регистрация с
   одного адреса за час — с провайдером 429 `captcha_required` без ответа (записи нет) и 201 с
   верным; без провайдера 429 `rate_limited` с `Retry-After`.
   `test_uc02_a5_login_ip_threshold_offers_captcha_with_provider`: шестая попытка входа с одного
   адреса — 429 `captcha_required`; с верным ответом неверный пароль → 401, верный → 200 + cookie.
   `test_one_captcha_answer_serves_both_login_thresholds` (провайдер `SingleUseCaptcha`: каждый
   ответ принимается один раз, обращения считаются): оба порога входа переступлены — верный
   пароль со свежим ответом → 200 при одном обращении к провайдеру; повтор ответа → 429.
   `test_one_captcha_answer_serves_registration_thresholds_and_setting`: пороги адреса и домена
   (домен — 200 отметками в Redis) и `captcha.enabled` — регистрация со свежим ответом → 201
   при одном обращении.
   `test_blocked_or_deleted_account_cannot_login_or_reset`: `blocked` → 401 без cookie,
   `login|denied`, ссылка восстановления не выдаётся; `deleted_at` → 401.
   `test_concurrent_registrations_of_one_address`: две одновременные регистрации → `[201, 409]`,
   одна запись, одно письмо.
6. `test_uc02_a6_a7_registration_modes`: `closed` → 403 `registration_closed`, `invite` → 403
   `invite_required`, `open` → 201.
7. `test_disposable_domain_and_captcha_settings`: домен из `disposable_email_domains` → 400
   `disposable_email`; `captcha.enabled` без провайдера → 400 `captcha_required` (fail-closed).
8. **TC-E2E-04** `test_uc15_reset_invalidates_sessions_and_link_is_single_use`: две сессии →
   `reset-request` → письмо `reset` → `reset-confirm` 200; обе сессии в Redis отсутствуют, старый
   пароль → 401, новый → 200; повторный переход по ссылке → 410 `token_expired` (AC-31).
9. `test_uc15_a1_unknown_address_is_indistinguishable`: по 12 запросов для зарегистрированных и
   для неизвестных адресов (по три на адрес — порог 3/ч) — статус, байты тела и все заголовки
   кроме `Date` одинаковы; расхождение медиан времени ответа < max(3 мс, 25 % медианы) при
   одном наборе запросов для обоих случаев (измерено: медианы 1,7–2,1 мс, расхождение ≤ 0,3 мс);
   для неизвестного адреса в очереди задачи `reset_unknown`.
10. `test_authenticate_costs_the_same_for_unknown_address`: время `authenticate` для неизвестного
    адреса отличается от известного менее чем вдвое (хеш-пустышка argon2id).
11. `test_uc15_a2_expired_and_foreign_links`: просроченная ссылка (`expires_at` в прошлом) → 410;
    токен подтверждения в `reset-confirm` → 410; пароль не изменён.
12. `test_uc15_a3_reset_request_rate_limit`: четвёртый запрос за час по адресу → 429
    `rate_limited` с `Retry-After` 1…3600, писем ровно три.
13. `test_logout_requires_csrf_and_revokes_sessions`: `logout` без `X-CSRF-Token` → 403
    `csrf_failed`, с неверным → 403, с верным → 204 и cookie сняты теми же атрибутами; сессия
    в Redis удалена; `logout-all` отзывает все сессии субъекта.
14. `test_login_rejects_when_authenticate_returns_none`: подмена → 401 без cookie (из 001.13,
    на клиенте со своим адресом).
15. `test_session_cookie_ttl_matches_declared`: `Max-Age` cookie и TTL ключа `sess:` — 30 суток.

Прочее: `test_security_fail_closed.py::test_database_outage_is_fail_closed_too` — закрытый порт
базы: `register`/`login` → 503 с `Retry-After`, не 500; `test_jobs.py`: тест неизвестного типа
в очереди `critical`, `test_scheduler_leadership_lock` проверяет память слотов `tick`;
`test_schema_accounting.py` — сравнение партиций «до/после» при живом планировщике.

## Модульные тесты

- **TC-UNIT-02** `unit/security/test_ratelimit.py` (tdd-strict, красные до реализации на
  `NotImplementedError`/`AttributeError`): шестой вызов при 5/60 с — ложь; ключи независимы,
  окно скользит; отклонённые попытки засчитываются; ключ истекает с окном; `peek` не засчитывает
  попытку и не видит отметки вне окна; `reset` обнуляет; `retry_after` — 58…60 с от самой
  старой отметки, 0 для пустого окна.
- `unit/security/test_sessions.py` (tdd-strict): `create` возвращает сессию с `csrf`,
  чтение со всеми полями; отзыв одной и всех кроме текущей; TTL продлевается при чтении;
  множество субъекта получает TTL сессии, продлевается при создании и чтении, мёртвые члены
  выбрасываются при создании.
- **TC-UNIT-01** `unit/domain/test_users.py`: `Foo.Bar+x@Example.com` → `foo.bar+x@example.com`,
  краевые пробелы; `hash_token` = SHA-256; `NoCaptchaProvider` отклоняет; `CaptchaAnswer`
  спрашивает провайдера один раз и никогда без ответа.
- `unit/jobs/test_queue.py::test_schedule_maintains_partitions_hourly`: `SCHEDULE` содержит
  `ensure_partitions` раз в час в `background`, обработчик зарегистрирован.
- `unit/security/test_stubs.py` скорректирован: ключи сессий и `RedisError`, `require_csrf` для
  безопасных методов и без сессии.

## Посадки стражей (до ревью, по одной; файлы восстановлены после каждой, `cmp` совпадает)

| Посадка | Результат |
| :--- | :--- |
| 1 ссылка многоразовая (`used_at` не проверяется) | e2e: «ссылка одноразовая (AC-31)», `200 == 410` |
| 2 вход без порога по адресу источника | e2e: `[401 ×6] == [401 ×5, 429]` |
| 3 `aup_version` не сохраняется | e2e: `'' == '2026-09'` |
| 4 сброс пароля не отзывает сессии | e2e: «сессии отозваны», сессия жива |
| 5 адрес без нижнего регистра | e2e: «хранится нормализованным (§16.8)» |
| 6 вход без подтверждения адреса | e2e: «вход до подтверждения адреса закрыт», `200 == 401` |
| 7 неизвестный адрес без задачи-пустышки | e2e: «тот же путь: задача есть», `0 == 3` |
| 8 окно на единицу строже (`>=`) | e2e: `[401 ×4, 429, 429]` |
| 9 CSRF не проверяется | e2e: `204 == 403` |
| 10 TTL сессии не продлевается | unit: «продлена при чтении», `5 > 3000` |
| 11 `SCHEDULE` пуст | unit: `KeyError: 'ensure_partitions'` |
| 12 неизвестный адрес без хеша-пустышки | e2e: «известный 27,1 мс, неизвестный 0,7 мс» |
| 13 планировщик без памяти о слотах | e2e: «слот уже ставился», `1 == 0` |

Посадка 12 при первом прогоне была зелёной (время ответа не охранялось) — добавлен тест 10.
Инцидент: цикл посадок под zsh (`for f in $FILES` без разбиения слова) прошёл без резервных
копий и посадки наслоились; восстановлено вручную, цикл перезапущен через `bash`.

## Раунд 2 — правки по ревью и их проверка

Ревью раунда 1 (`sarcasmotron-001-14`, REJECTED): две посадки ревьюера прошли зелёными — C-1 вход
заблокированной/удалённой записи не охранялся; C-2 страж времени UC-15 A1 с порогом 0,1 с
пропускал задержку 60 мс; S-1 порог по учётной записи считал все попытки и без провайдера CAPTCHA
запирал владельца чужими пятью запросами в минуту (§5.12 запрещает блокировку учётной записи);
S-2 две одновременные регистрации → 500; S-3 `user_sessions:<subject>` рос вечно; L-1 cookie
`csrf` могла уехать пустой; L-2 реакция §5.12 (CAPTCHA / «отказ с задержкой») подменена отказом
без объявления; D-1 протухший комментарий «до уточнения в 001.14»; D-2 судьба задач `send_email`
до обработчика; N-1 (вкусовщина: EVAL вместо EVALSHA, приватные поля в тестах, `+тег`).

- **C-1:** `test_blocked_or_deleted_account_cannot_login_or_reset` — `status = 'blocked'` →
  вход верным паролем 401, `auth_events` `login|denied`, `reset-request` без ссылки
  (`reset_unknown`); `deleted_at` → 401. Посадка ревьюера (без `status == 'active'`) → `200 == 401`.
- **C-2:** `request_reset` выполняет один набор запросов для любого адреса (`INSERT … SELECT`
  токена по условию + одна задача `send_email`), страж сравнивает медианы по 12 запросам на
  случай: расхождение < max(3 мс, 25 % медианы); измерено на стенде: медианы 1,7–2,1 мс,
  расхождение ≤ 0,3 мс. Посадка ревьюера (`sleep(0.06)` на ветви известного адреса) →
  «известный 65,7 мс, неизвестный 5,0 мс».
- **S-1:** `RateLimiter.peek`/`reset`/`retry_after` (tdd-strict: три теста красные до
  реализации); вход читает счётчик учётной записи до пароля, засчитывает только отказ, обнуляет
  успехом; над порогом с провайдером CAPTCHA проверяется до пароля, без провайдера порог не
  действует (объявлено в задаче как компромисс до ОВ-A3). Тесты: владелец после десяти чужих
  неудач входит и счётчик обнулён, успешные входы порог не тратят; с провайдером (подмена
  зависимости `get_user_service`) — 429 `captcha_required` до пароля, верный ответ → 200,
  верный ответ с неверным паролем → 401 и отказ засчитан. Посадки: без `reset` → «успех обнуляет
  счётчик, 1 == 0»; счётчик на каждой попытке → «десять отказов засчитаны, 20 == 10»; без
  проверки CAPTCHA над порогом → `200 == 429`. Стенд через nginx: 12 чужих неверных паролей →
  владелец 200, счётчик обнулён.
- **S-2:** `INSERT … ON CONFLICT (email) DO NOTHING RETURNING id` → `None` = 409;
  `test_concurrent_registrations_of_one_address` (`asyncio.gather`) → `[201, 409]`, одна запись,
  одно письмо. Посадка (без `ON CONFLICT`) → `500 == 409`.
- **S-3:** множество субъекта получает TTL сессии (`EXPIRE GT`/`NX`, продление при `get`) и при
  создании очищается от членов без живой сессии (проверка `EXISTS` конвейером);
  `test_subject_set_expires_and_drops_dead_members` (tdd-strict). Посадки: без TTL → `3500 < -1`;
  без чистки → «мёртвый член выброшен».
- **L-1:** `SessionStore.create` возвращает `Session` (идентификатор и `csrf` без повторного
  чтения); посадка (пустая cookie `csrf`) → красная в полном цикле UC-02.
- **L-2:** `limit()` при отказе ставит `Retry-After` = секунды до выхода самой старой отметки из
  окна; при настроенном провайдере CAPTCHA превышение порога регистрации → 429
  `captcha_required`; трактовка «отказа с задержкой» объявлена в задаче. Тесты A5 и A3 сверяют
  `Retry-After`; посадка (без заголовка) → `KeyError: 'retry-after'`. Стенд: шестой запрос с
  одного адреса → 429, `retry-after: 60`.
- **D-1:** комментарий `USER_SESSION_TTL` заменён ссылкой на решение. **D-2:** требования к
  обработчику `send_email` записаны в задачу 001.78 (`reset_unknown` без письма, просроченные и
  использованные токены не рассылать, токен не журналировать) и в «Уточнения» 001.14.
- **N-1:** Lua-скрипты регистрируются один раз (`register_script` — EVALSHA с запасным EVAL);
  приватные поля в tdd-тестах и `+тег` в нормализации оставлены (контракт TC-UNIT-01).

Посадки раунда 2 (10 новых, все красные, файлы восстановлены — `cmp` совпадает):

| Посадка | Результат |
| :--- | :--- |
| 14 вход без проверки `status = 'active'` | e2e: «заблокированная запись не входит», `200 == 401` |
| 15 `sleep(0.06)` на ветви известного адреса | e2e: «известный 65,68 мс, неизвестный 5,03 мс» |
| 16 регистрация без `ON CONFLICT` | e2e: `500 == 409` |
| 17 успех не обнуляет счётчик учётной записи | e2e: «успех обнуляет счётчик», `1 == 0` |
| 18 счётчик тратится каждой попыткой | e2e: «десять отказов засчитаны», `20 == 10` |
| 19 над порогом CAPTCHA не спрашивается | e2e: `200 == 429` |
| 20 множество сессий без TTL | unit: `3500 < -1` |
| 21 мёртвые члены не выбрасываются | unit: «мёртвый член выброшен, живые на месте» |
| 22 отказ без `Retry-After` | e2e: `KeyError: 'retry-after'` |
| 23 пустая cookie `csrf` при входе | e2e: значение cookie ≠ `''` |

После правок: `make check` → 0, **187 passed** (180 + 7 новых); стенд пересобран (`up -d
--build`), все роли `Up`, `api` healthy; сквозная проверка через nginx повторена.

## Раунд 3 — правки по ревью раунда 2

Ревью раунда 2 (`sarcasmotron-001-14`, REJECTED): обе посадки раунда 1 красные; C-1 ветка
реакции CAPTCHA в `limit()` не охранялась (посадка «блок удалён» — 64 passed); L-1 порог входа
по адресу источника не предлагал CAPTCHA даже с провайдером (§5.12 называет оба измерения);
D-1 разделы отчёта до «Раунда 2» описывали прежний код; N-1 (цена argon2id для занятого
адреса, точность score в float64).

- **C-1:** `test_uc02_a5_registration_threshold_offers_captcha_with_provider` — с провайдером
  шестая регистрация с адреса → 429 `captcha_required` (записи нет), с верным ответом → 201; без
  провайдера → `rate_limited` с `Retry-After`. Посадка ревьюера (блок реакции удалён) →
  `'rate_limited' == 'captcha_required'`; посадка «любой ответ пропускает» → `201 == 429`.
- **L-1:** вызов `LOGIN_IP` получил `captcha=`;
  `test_uc02_a5_login_ip_threshold_offers_captcha_with_provider` — шестая попытка → 429
  `captcha_required`, с верным ответом неверный пароль → 401, верный → 200 + cookie. Посадка (без
  `captcha=`) → `'rate_limited' == 'captcha_required'`. Объявлено в задаче и картах.
  Нумерация посадок раунда 3: 24 — блок реакции CAPTCHA в `limit()` удалён (посадка
  ревьюера), 25 — `LOGIN_IP` без `captcha=`, 26 — с провайдером любой ответ пропускает
  (`201 == 429`).
- **D-1:** разделы «Регрессия», «Сквозные тесты», «Модульные тесты» приведены к текущему коду.
- **N-1:** комментарии о цене хеша для занятого адреса и о точности score добавлены.

После правок: `make check` → 0, 189 passed; стенд пересобран, все роли `Up`.

## Раунд 4 — правки по ревью раунда 3 (лимит раундов workflow исчерпан)

Ревью раунда 3 (`sarcasmotron-001-14`, REJECTED без CRITICAL): обе посадки раунда 2 красные;
S-1 — один ответ CAPTCHA проверялся у провайдера дважды за запрос, когда переступлены оба
порога входа (адрес источника и учётная запись); ответы настоящих провайдеров одноразовые,
вторая проверка отвечала бы «уже использован» — владелец за общим адресом остался бы заперт
(доказано ревьюером инструментальным прогоном: 2 обращения за один запрос). N-1: список тестов
в отчёте по номерам; кортеж вместо именованного типа.

- **S-1:** `CaptchaAnswer` (`domain/users.py`) — ответ запроса с однократной проверкой; `limit()`
  принимает его вместо кортежа; регистрация передаёт тот же ответ в `UserService.register`
  (настройка `captcha.enabled` — третье место, требующее ответа). Тесты с провайдером
  `SingleUseCaptcha`: вход над обоими порогами → 200 при одном обращении, повтор ответа → 429;
  регистрация над порогами адреса и домена при `captcha.enabled` → 201 при одном обращении;
  unit-тест мемоизации. Посадки: 27 — проверка при каждом обращении (сценарий ревьюера) →
  `429 == 200`; 28 — регистрация строит второй ответ из токена → `400 == 201`. Требование к
  будущему провайдеру записано в «Уточнениях» (ОВ-A3).
- **N-1:** `CaptchaAnswer` вместо кортежа; список тестов отчёта — по именам.

После правок: `make check` → 0, **192 passed**; стенд пересобран, все роли `Up`.

### Вердикт раунда 4 (разрешён владельцем сверх лимита workflow)

`sarcasmotron-001-14`: **APPROVED**. Обе посадки ревьюера красные (снятая мемоизация валит два
сквозных теста и unit-тест, `2 == 1`; собственный ответ в `register` → `400 == 201`);
инструментальный прогон ревьюера с одноразовым провайдером над обоими порогами — одно обращение
к провайдеру вместо двух, владелец входит. CRITICAL, логика, безопасность, slop — нет. Вкусовщина
без действий: `CaptchaAnswer.available` и `UserService.captcha_available` считают одно и то же
(оба используются); совместимый путь `register(captcha_token=…)` без вызовов (контракт 001.13);
`+тег` в нормализации — открытый вопрос §16.8, записан в «Уточнения» задачи.

## Критерии приёмки

- [x] AC-31: ссылка восстановления одноразовая, повтор отклонён — тест 8, посадка 1, стенд
- [x] AC-36: превышение порога даёт отказ с сохранением доступности остальным — тест 4 (другой
      адрес источника входит), посадки 2 и 8
- [x] Регистрация без принятия правил невозможна (`aup_version` обязателен → 422); версия
      сохранена (AC-23) — тест 2, посадка 3
- [x] Сессии инвалидируются в срок Н-27 — `revoke_all` удаляет ключи немедленно (тест 8,
      посадка 4); блокировка учётной записи — операция админ-API следующих задач

## Отклонения от описания задачи

- Токен письма лежит в полезной нагрузке задачи `send_email` (тесты берут его из очереди, не из
  «перехватчика»); обработчик — 001.52, до него задачи `pending`.
- Режим `invite` = 403 до задачи кодов приглашения; CAPTCHA без провайдера — fail-closed.
- Пороги — константы (`REGISTER_DOMAIN` 200/ч, остальные по §5.12); порог по учётной записи
  считает только отказы и без провайдера CAPTCHA не действует (компромисс до ОВ-A3); реакция
  «отказ с задержкой» = 429 с `Retry-After`; адрес нормализуется целиком в нижний регистр; срок
  ссылок 24 ч / 1 ч (ОВ-25, умолчания).
- Сверх контракта: `confirm_reset_user`, именованные `ip`/`user_agent` у `authenticate`,
  `request_reset(lang=)`; `DatabaseUnavailable` → 503; миграция `100_settings_auth`;
  `ensure_partitions` в `SCHEDULE` и память слотов планировщика (частично 001.37; исправление
  дефекта 001.11, обнаруженного на стенде); адаптация `test_schema_accounting` и `test_jobs`.
  Всё объявлено в «Уточнениях при реализации» задачи; карты `app/.AGENTS.md`,
  `app/api/.AGENTS.md`, `control-plane/.AGENTS.md`.
